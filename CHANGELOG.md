# 更新日志

**读者是 agent 与开发者。** 这里是完整的技术记录：每个版本的根因、实现取舍、踩过的坑、
以及当时怎么验证的，都写在这 —— 接手的人（或 AI）想知道"为什么是这样"，先读这里。

**给用户看的那一份在 `lib/changelog.ts`**（App 设置面板里的「更新了什么」）：只讲
新增 / 修复 / 优化了什么、对用户有什么影响，**不讲为什么**。
**两处口径不同、不许混写** —— 同一件事在这里把原因写透，在那边只写结果。

版本号唯一来源是 `package.json` 的 `version`，三处必须同步。

递增规则偏克制：
- 成块的新功能（新增完整模块 / 页面 / 能力）→ MINOR
- 其余一切（修 bug、样式、文案、加个按钮）→ PATCH
- 只改文档 / 注释 / 构建脚本 → 不升版本
- 拿不准就选小的那个

---

## [1.5.0] - 2026-09-22

**添加糖追踪 + 自做饭菜：把「算不出来的那一维」和「自己做的饭」补上。**

### 添加糖：一个被明确否决过的维度，为什么这次能加

`lib/nutrition/score.ts` 的头注释里原本白纸黑字写着：
「算不出来的维度不许编。食物库没有『添加糖』字段，所以这里没有『添加糖』这一维」。
那句话当时是对的 —— 全库 grep 不到任何 `sugar` 字段，硬加一维就是假精确。

变的是**数据来源**：包装食品的营养成分表按现行国标**强制单列「糖」**，
而 1.4.0 之后 app 已经能拍照识别那张表。糖第一次有了真实、可核对的来源。

所以这一维不是「新增」，而是**把代理指标升级成真数据**：
「零食甜饮」原本按食物名字筛（`SWEET_DRINK_HINTS`），有糖数据时改走
「添加糖供能占比」（5% 满分 / 15% 归零），没有时保留原代理。
**维度数、权重、key 一个没动** —— 加第八维会和这一维双重扣分（一罐可乐罚两次），
还要重分七维权重，那是另一件事。

### 糖的缺失语义与钠**不同** —— 这次最容易做错的一处

钠缺了 = **不知道**含多少 → 求和时跳过，当 0 是撒谎。
添加糖缺了 = 按「添加糖」的**定义**，这个配料**就没加糖** → 当 0 是对的。

于是 `addNutrition` 对糖用**同一个** `opt`，一行特例都不写：
一个配料有糖、另一个没标 → 结果 = 有糖那个；**两个都没标 → `undefined` 而不是 0**。
最后那条边界是「今天没吃甜的」与「今天一条都没标糖」的区别，
界面配套的规矩是只能说「**已记录的**添加糖 Xg」。

⚠️ 千万别把它「修」成 `(a.sugar ?? 0) + (b.sugar ?? 0)` —— 那会破掉第二条边界。

### 内置库只给纯糖类补了两条

`baitang`（白砂糖，`sugar: 99.9`）与 `fengmi`（蜂蜜，`sugar: 75.6`），
其余 **248 条一条不动** —— 这两条的糖值是**定义性**的（蔗糖纯度、蜂蜜含糖量），
不需要查资料，与「不影响现有体系」并不冲突。
不补的话，用户点了「＋ 一勺糖」看到的「添加糖 0g」是个假数字。

**明确不做**：用「碳水」推糖。（纯糖能推，同一条判据套到米饭上就是假精确。）

### 目标口径

中国居民膳食指南 2022：添加糖每天**不超过 50g**，**最好控制在 25g 以内**。
两个数都进了 `lib/nutrition/targets.ts`（`SUGAR_IDEAL_G` / `SUGAR_LIMIT_G`）。

### 自做饭菜：数据形状刻意照抄 `data/foodRecipes.json`

配方是 `{ foodId, yieldG, parts: [{ foodId, grams }] }`，与内置成品菜同一套算子
（`recipeTotals` / `recipePer100` / `recipeToFoodItem`，都在 `lib/nutrition/recipe.ts`）。
那个文件的头注释明写「这个文件里一次 `* k` 都不该出现」——
所有折算走 `nutritionOf` / `addNutrition` / `scaleNutrition`，不新写第二套乘法。

三个取舍：

1. **成品重量默认 = 配料总重**（用户可改）—— 炒菜会失水，但「默认按总和」是可解释的起点；
2. **记账时可选展开成多条或合成一条** —— 展开能看到每种配料，合成更接近「我吃了这道菜」；
3. **菜谱 id 用 `recipe-<uuid>` 前缀** —— 顺带排除与内置库 id 碰撞。

### 验收后的四处修复

1. **`addNutrition` 必须加 `sugar: opt(...)` 一行。** 交接书原写「不改这个函数的代码」，
   但那个前提不成立：它是**逐字段手写的返回字面量**，根本没有 `sugar`。
   照字面执行会**静默丢糖** —— 而做菜加的糖正是糖的来源之一。
   真正该守的是 `opt` 表达式不动（核对两版逐字相同）。
2. **`recipePer100` 的守卫从 `> 0` 收紧为 `Number.isFinite`。**
   `Infinity > 0` 为真 → `100 / Infinity = 0` → 整道菜被静默折成「每 100g 全 0 热量」，
   一个看起来完全正常却根本不对的数字。由新单测抓到。
3. **`myRecipes.persist` 读回来比内容不比长度。** 本模块比 `customFoods` 多一个「改」动作，
   改一条菜谱**条数不变**，比长度的话「写失败了」和「写成功了」读回来一模一样。
4. **`nutritionOf` 缺糖数据时不写这个键**（而非 `sugar: undefined`）。
   `JSON.stringify` 会把 undefined 键丢掉，于是「刚记下的快照」与「重开读回来的」形状不同
   （`diet.test.ts` 那条断言实测红过）。

### 验证

`npm test` 473/473 · `check:data` 18 项 · `check:nutrition` 250 条无问题 ·
**自证模式七处破坏全被拦下** · `check:reference` 0 · `eslint` 0 ·
`build` 7 路由全静态 · `verify-subpath` 8 页 166 链接 · `check:chunks` 8 页全对 ·
`smoke` 22 条浏览器断言全过。

### 同一版里的其它改动

- 饮食页顶部直接给「📷 拍照添加」，不必先搜索再拍照；
- 拍照识别尽量读出包装上的完整商品名（含品牌）；
- 删掉「申请进正式库」里那句不准确的提示（正文其实不含邮箱）；
- 修掉 smoke 样例数据的「时间炸弹」：`legacy-v1.json` 的打卡日期写死，
  导致周报那条断言到 2026-09-21 必然变红。现在日期键会被平移到本周。
## [1.4.0] - 2026-09-20

**拍照识别食物：把「库里没有、给不出结论」的东西补上。**

起因是用户的一句话 —— 食物库里查不到的食物，app 只能回一句「没搜到」，
而他要的是"拍一张照，AI 读出数值并加进去"。

### 为什么不需要接外部 agent

查证后确认：DeepSeek 开放平台**现在支持图片输入**（模型 `deepseek-flash`），
而它的限制表（单边 ≤8192px、单图 ≤32MiB、请求体 ≤48MiB、15+ 张时降到 4096、
每图自动缩到约 1300×1300、≤1024 tokens/图）**与地雷 6 逐字吻合** ——
那条地雷当初就是照这份文档写的：路早就规划过，只是没接线。
所以识图在 app 内闭环即可，不必把本机 agent 接进来。

**不做直连本机 agent 的三个理由**（写下来，免得以后有人再提）：

1. **手机场景不通** —— 站点是 https，请求 `http://127.0.0.1` 会被**混合内容**拦掉，
   而且手机上那个地址是手机自己；
2. **安全** —— 让一个公网页面能驱动"读文件、改代码、跑命令"的进程，等于开一个远程执行面；
3. **不必要** —— agent 不可替代的只剩"写进正式库并发布"，那一环用一键预填 Issue / 邮件交接即可。

### 三个刻意的取舍

1. **视觉只做转录，不做估算 —— 而且不信模型自觉。**
   prompt 要求「看不清就留空、不许推测」，但**代码层**还兜了一道：
   `lib/ai/foodVision.ts` 在 `kind === "dish"` 时**直接抹掉所有数值字段**。
   模型一边说"这是一道菜"一边给出热量时，那个热量进不来。

2. **读到的数字要过三道校验才被允许成为数值**（`lib/nutrition/verify.ts`，纯函数）：
   Atwater 闭合（蛋白×4 + 脂肪×9 + 碳水×4）+ NRV 反算 + 数值区间。
   任一不过就 `reject`，**一个数字都不给**，只让用户重拍。
   这不是洁癖：实测同一条 `10.5 g` 在低分辨率的手机照片里会被 OCR 读成 **105 g** ——
   十倍错误，而它**看起来完全正常**，只有拿去做闭合校验才会发现 1785 ≠ 153。

3. **用户食物独立于静态库。** `data/foods.zh.json` 是构建期资产，运行时改不了；
   所以用户自己加的食物存在 `recipe.customFoods.v1`，检索时两边合并。
   ⚠️ 合并检索 `lib/nutrition/lookup.ts` **复用 `bestNameMatch`**，一行匹配逻辑都没新写 ——
   地雷 29 就是"闸门自己抄了一份判据、判据漂了它却全绿"，这条路上不能再有第二份判据。

### 落库仍然只有一条路

用户食物就是一个 `FoodItem`，落库走 `recordDietEntry({ food })` → `makeDietEntry` → `nutritionOf`，
**没有新增任何计算路径**。刻意**不用** `recordCustomEntry` —— 它内部自己算 `per100 * k`，
等于复制了"数字的唯一来路"。

AI 估算的条目在**三处**都标出来：库条目的 `source` 写「AI 估算（仅凭外观推测，不可核对）」、
确认页有警告卡、记账时 `DietEntry.source = "ai"`（于是当日汇总能说清"其中 N 条是猜的"）。

### 踩到的坑

- **Git Bash 下 `BASE_PATH` 会被 MSYS 路径转换咬。** `BASE_PATH=/yuanqi-ledger npm run build`
  里那个路径被转成了 `D:/Git/yuanqi-ledger`，Next 直接报 `Specified basePath has to start with a /`。
  而 `MSYS_NO_PATHCONV=1` 加在 `npm run` **前面不生效** —— npm 会再起一层 shell，变量没传下去。
  必须写成：
  ```bash
  env MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*' BASE_PATH=/yuanqi-ledger npx next build
  ```
  判据：`out/index.html` 里应当出现 122 次 `/yuanqi-ledger`。
- **`eslint.config.mjs` 的 `globalIgnores` 没覆盖 `.tmp-*`。** 按项目约定（`.gitignore` 的
  `/.tmp-*`）把临时脚本放 `.tmp-*` 里，会**污染 eslint 基线** —— 而本项目的基线是 0 错 0 警。
  验收时踩到一次（一个未使用的常量就让它变成 1 warning）。

### 怎么验证的

九条命令全绿：`eslint` / `build` / `verify-subpath` / `check:data` / `smoke` /
`check:nutrition` / `check:reference` / `npm test` / `check:chunks`。
外加**三个闸门的自证模式**（`YQ_SELFTEST=1`）：`check:nutrition` 六处破坏、
`check:reference` 两处、`check:chunks` 一处 —— 全部被对应检查拦下，证明它们不是永远绿。
`npm test` **428/428**。

**仍未实测的一环**（如实记下）：`scripts/probe-vision.mjs` 的联网部分需要 Key，
所以 `json_object` 与图片块能否同用、`deepseek-flash` 的 thinking 模式怎么关、
以及 `deepseek-chat` 是否仍可用，这三条目前是**按官方文档写的假设**，不是实测结论。
⚠️ 其中第三条值得警惕：官方当前的模型表里只有 `deepseek-flash` / `deepseek-v4-pro`，
`deepseek-chat` **不在表里**，而它仍是「帮我挑」那条路的默认模型名。

---

## [1.3.3] - 2026-09-19

**补录的运动记录会插到列表最前 —— 排序取的是录入时间，而不是运动日期。**

`ExerciseRecord` 上有两个时间：`date` 是**运动发生的日期**，`at` 是**录入那一刻的时间戳**。
而 `addExercise()` 写的是 `[{ 新记录 }, ...已有]` —— 新记录**无条件插到最前面**，
列表因此成了"补录序"。补录一条上周三的运动，它会出现在今天上面。

`lib/exercise.ts` 里其实有个 `sortExercises()`（按 `date` 倒序、同日按 `at` 倒序），
但**全仓零调用方** —— 从写下那天起就是死代码，从未参与过排序。

### 改了什么

- `loadExercises()` 与 `addExercise()` 都过一遍 `sortExercises()`。
  **只修写入侧不够**：设备上已有的记录就是修复前写下的乱序数据，必须在**读取时**一并纠正。
- 修在数据层而不是界面层：需要顺序的只有运动卡；weekly 与徽章墙只取统计值
  （运动时长合计），与顺序无关。改一处即可覆盖全部消费方。

### 验证

- 新增 `lib/exercise.test.ts` 5 条，其中 3 条专门盯顺序（补录排序 / 存量乱序 / 写入有序）。
- **回退自证**：把修复临时撤掉后重跑，**5 条挂 3 条** —— 正是那 3 条顺序断言。
  证明这套断言真的拦得住这个 bug，不是空跑。

### 顺带

- App 内的版本说明（`lib/changelog.ts`）改为结果导向：31 个版本、99 条说明只讲
  新增 / 修复 / 优化，根因与实现细节全部移回本文件。版本号、日期、条数结构未动
  （新旧各解析一次逐条比对过）。写作口径写进 `AGENTS.md` §6。

---

## [1.3.2] - 2026-09-19

**再接一次真机：增量复制没有先建目录 —— 从第二个版本起，更新必然失败。**

1.3.1 修好了"产物基址"，可真机上再跑一次更新又失败了。设置面板如实写出了原因：

```
Missing parent directory – possibly recursive=false was passed or parent
directory creation failed.
```

根因：Capacitor 的 `CopyOptions` 只有 `from` / `to` / `directory` / `toDirectory`，
**没有 `recursive`**（只有 `writeFile` 与 `mkdir` 有）。而无感更新是**增量**的：
上一版里没变的文件要**复制**到新目录，可新目录还是空的 —— `copy` 直接抛错。

**为什么第一次测的时候没暴露**：那次设备上还没有任何旧版本目录可复制，
`collectPresent` 返回空、`copy` 一次都没走到。等到设备上留下一个完整的旧版本目录
（它会把 `ota/` 下所有版本目录的本地清单合并起来用），这条路径才第一次被执行。

这是典型的"**第二次才炸**"，而且它恰好是**最常走**的那条路 —— 只要设备上有上一版，
就一定会走增量复制。修法是复制前先建好目标文件的父目录（同一目录只建一次）。

### ⚠️ 一条来自这次的教训

**这个 bug 在壳（安装包）代码里，所以它自己修不了自己** —— 只能出一个新的安装包。
无感更新"第一次上线"因此格外危险：如果错在第二个版本才炸，用户就只能再装一次包。
所以真正的验证必须**至少走两轮**：装机 → 更新到下一版 → 再从那一版更新到再下一版。

---

## [1.3.1] - 2026-09-19

**修掉「能下下来、但切过去是一张裸 HTML」的问题 —— 无感更新交付的产物拿错了。**

1.3.0 上线后在真机上完整走了一遍才发现：壳下载到的东西**清单是对的、字节也一个不差**，
但那是**给网页版用的产物**。网页版部署在子路径下，样式与脚本的引用都带着
`/yuanqi-ledger/` 前缀；而壳里的站点根就是 `/`，于是切过去之后：页面内容加载出来了、
样式和脚本全 404 —— 用户看到的是一张没样式的 HTML。

**这一条骗过了所有本地检查**：构建全绿、七道闸门全过、连"清单里的 86 个文件逐个
下回来核对 sha256 全部一致"也放行。**「清单对」不等于「产物能在壳里跑」** ——
只有真机端到端才暴露。

### 改了什么

- 发布时**分开构建两份产物**：一份给网页版（子路径），一份给壳下载（根基址）。
  壳拿到的那份与将来打进安装包里的东西**完全同构**，不再靠"看起来差不多"。
- 清单里多一个 `base` 字段，说明这份产物在站点的哪个目录下。
- 加了一道**发布前断言**：扫产物里有没有站点子路径前缀，有就中止发布；
  与打安卓包时守"白屏"的那道断言是同一套判据。
- 发布后核验脚本也多查一层：光核对"字节与清单一致"不够，还要确认入口 HTML
  是按根路径引用的 —— 这次的问题恰好躲过了字节核对。

### 代价

一次发布要跑两次前端构建，比从前慢一倍。

### 另外

回滚保险（新版起不来就自动退回上一版）在这次事故里第一次被真机走到：
手机上那个包冷启动后**自己回到了旧版**，记录完好。

⚠️ 但**不能据此说"这条保险起作用了"**：还有第二种解释 —— 基址本来就没持久化成功，
冷启动自然回到安装包内版本。**两种解释在外部观察上完全一样**
（release 包不是 debuggable，看不到应用私有目录里的标记文件与基址配置）。
要坐实得再打一次**可调试**的测试包，或者把当前基址显示到界面上。

---

## [1.3.0] - 2026-09-19

**安卓 App 可以「不换安装包」更新了（无感更新）。**

起因很直接：网页版点一下就更新，安卓 App 却要走「下载新的安装包 → 再装一遍」。
这一版给壳加了自更新：App 自己在后台把新版**界面资源**下到应用私有目录，校验通过后
用户在设置里点一下「立即更新」就切过去 —— **安装包不用换，记录也不会丢**。

### 一、怎么工作

- 站点发布时生成一份**资源清单** —— `version.json` 里新增 `ota.files`，
  记下每个文件的路径、字节数与 sha256（`version` / `apk` 等既有字段一个没动，老版本照常读）。
- App 启动后（延迟一会儿，别和首屏抢带宽）和设置面板里点「检查更新」时各比一次版本号。
  有新版就**下载，但不切换**。
- 下载是**增量**的：文件名带哈希，没变的文件直接从上一版目录复制过去，不重复下载。
- 每个文件都**先校验字节数与 sha256**，全部通过才把临时目录改名成正式目录 ——
  **半个包绝不上线**（半成品目录一旦被壳选中就是白屏）。
- 用户在设置里点「立即更新」才切过去，并清掉旧缓存。

### 二、装坏了能自己退回来

切换前先落一个「待确认」标记，新版本渲染成功之后才删掉它。
新版本若白屏，**网页侧代码根本跑不起来**，所以这一步交给壳里那十余行 Java：
冷启动时发现标记还在（= 上次切完没等到确认），就把基址退回上一版
（没有上一版则退回安装包内的资源），并把出问题的那一版目录隔离掉，免得反复触发。

### 三、边界

- **不换安装包 ≠ 不用换安装包**。装了新的安装包会自动回到「基线」，
  自更新只服务「基线到线上之间的差」。
- 关掉 App 也能收到的远程推送、以及静默安装，仍然不做（理由见 `docs/HANDOFF-OTA-UPDATE.md` §6）。
- 这一版**必须先装一次新的安装包** —— 老安装包里没有这段代码，指望不上它自己更新。

---

## [1.2.0] - 2026-09-19

**首页多了一张卡：「今天吃什么」—— 从你自己的菜单库里挑一道。**

它的理由很直白：**别的推荐都要求"今天已经记过东西"**。
首页原来那张「今天还该吃点啥」是**缺口驱动**的：今天没有饮食记录时它显示"先去记一笔"，
各项都达标时它说"不用特意补"，走模型那条路还要填 Key。
也就是说 —— **"我现在要吃饭了，吃什么"这个时刻，界面上一个字都没有。**
而菜单库里那 194 道菜，除了被拿去算缺口，从来没有人问过"这顿吃哪道"。

### 一、挑一道

- 从**你自己的菜单库**里挑**一道**，给一句为什么推它（「你上次点它是 12 天前」「你菜单里还没点过这道」）。
- **「换一个」**：不重复刚才那道。换到的菜会记进本次会话的"已出过"，
  排完了就从头再来一轮（并如实说一句）—— **「换一个」永远不会走进死路**。
- **「就吃这个」**：直接把这道菜记进饮食日记，连带**记到哪一餐**（默认按现在钟点猜，随时可改）。
- **不需要任何前提**：不用先记账、不用填 Key、断网也能用。这条路径**一个 API 都不调**。

挑选规则只有两个信号，都是能解释的，没有口味模型、没有场景推断：
**多久没吃了**（3 天内降到 15%，越久越接近 1；这是软降权，不是硬排除）与
**点过几次**（常点的最多 +30%）。再叠一个由 `roll + 菜 id` 散列出来的抖动，
所以**同一个随机值必然同一道菜**（可单测），而换一个随机值就换一道。

### 二、估不出热量的菜照样能推

菜单库里**约八成是套餐长名**（「香辣鸡腿中国汉堡+塔塔鸡块(3块)+翅根+冰柠可乐」这种），
`estimateDish` 天生认不全 —— 这是 0.6.1 就查清并记在 ROADMAP 里的事。
把它们排除掉的话，"今天吃什么"只能在那两成里兜圈子，推荐会反复推同样几道。

所以这一版的口径是：**推荐只负责"决定吃哪道"，本来就不需要热量**。
- 估得出来的 → 照旧给区间；
- **估不出来的 → 照样能推，但一个数字都不显示**，旁边直接给一条出路
  （「就是『米饭』」这种一键关联就摆在结果里，点一下这道菜就记得住了）；
- 这种菜**不给「就吃这个」**：它的食材拆不干净，记下来只会漏算 —— 那正是它被判成估不出来的原因。

### 三、连带改的一处数据契约（加法式，不新增键）

要回答"最近吃过哪几道"，得让饮食记录知道"这条是从菜单库的哪道菜记下来的"。
`DietEntry` 因此多了一个**可选**字段 `dishId`：

- 一道"拆食材"的菜会展开成多条记录（走的是「一顿饭」预设那条既有做法），
  这几条**带同一个 `dishId`**，所以"吃过没有"看的是它，不是条数。
- ⚠️ **1.2.0 之前的记录一条都没有这个字段，那是正常状态**，不回填、不报错，
  也**不算"吃过"** —— 要是把"库里有记录"当成吃过，全库的菜会一夜之间变成"最近刚吃过"。
- **没有新增任何 `recipe.*` 键**，所以备份导出/导入/清空的覆盖范围不用动，
  `lib/storage/keys.ts` 与 `BACKUP_GROUPS` 一行都没改（这也是选它而不是另开一个"最近点过的菜"键的理由：
  两个数据源必然漂移）。

⚠️ 还有一处是**刻意不合并的**：吃完之后"吃过什么"**不会**立刻重算 ——
否则那道菜的新鲜度当场掉下去，卡片会在你眼前换成另一道，而提示还写着"已把某某记到午餐"。
它只在**装载**与**点「换一个」**时重读。

### 验证

- 单测 273 → **295**（新增 `lib/nutrition/pickDish.test.ts` 22 条）：
  同输入同输出 / 换 roll 会换菜 / **枚举 200 个 roll 忌口那道一次都不出现** /
  收窄只收本类 / **刚吃过 2 天的必输给没吃过的（100 次全中）** /
  **20 天前吃过的能赢（证明是软降权不是硬排除）** / 候选排完就放宽并如实报 `relaxed` /
  日期坏掉时退回"从没吃过"而不是让这道菜永远排最后 /
  **长套餐名（先用 `estimateDish` 断言它真的是 `none`）照样能被选中** / `daysBetween` 跨月跨年与坏输入。
- 冒烟 16 → **17**（`checkPickDish`，13 条断言）：**没有任何饮食记录时也给出推荐** /
  能选记到哪一餐 / **只剩忌口那道时也不推它** / 「换一个」真的会换 / 刚吃过的那道不再被推 /
  「就吃这个」落库且**每条都带 `dishId`** / **落库的正是屏幕上那道菜** /
  **记下的热量等于屏幕上那个数** / 菜单库空时说清是空的 /
  **估不出来的菜不显示热量、也不给记录按钮**。
  ⚠️ 两道"可估"的 fixture 是**手动关联到食物库**的（`foodId` + `grams`），不靠"菜名拆得出来"——
  否则食物库一改，fixture 会悄悄变成"估不出来"，而断言还绿着。
- **自证（两轮，共五处破坏）**，每一处都确认报出了**对应的那一条**：
  ① 去掉忌口过滤；② 「换一个」什么都不做；③ 记录不写 `dishId`；④ 给估不出来的菜编一个数；
  ⑤ 只有"今天记过东西"才肯说话。恢复后重跑全绿。
  ⚠️ 自证当场挖出**我自己的检查有问题**：「忌口那道从不出现」原来是"换一个 12 次、看抽到的名字里有没有它"——
  去掉忌口过滤之后它**照样全绿**（3 道菜里随机 12 次恰好没抽中）。
  靠抽样去碰的断言等于没有断言，改成"让撞忌口的那道**成为唯一候选项**"之后，过滤在不在就是必然结果。
- 其余闸门照跑：eslint 0 错 0 警 / build / check:data（**15 → 16 项**，新增
  「老记录没有 dishId」）/ check:nutrition / check:reference / check:chunks / verify-subpath —— 全绿。

---

## [1.1.1] - 2026-09-19

**补一段界面上的断头路：网页版里终于有地方能拿到安卓安装包了。**

起因是一个很直白的问题：「网址里面安装的是 apk 吗？」—— 不是。
网址打开的是**网页版（PWA）**，安卓 Chrome 里那个「装到桌面」装的是一个 Web App，
**不是**我们签名打包的那个 `.apk`。而 1.1.0 做的「应用内更新」里，
「下载新版」这个按钮**只在安卓壳里出现** —— 也就是说：**已经装了的人能看到，还没装的人永远看不到**。
第一次怎么装，界面上原本一个字都没有，只能靠知道那个 `/apk/...` 地址。

### 改了什么

- **设置面板底部新增一条「安卓 App（安装包）」**（`AndroidApkSection`），
  只在**网页版 + 安卓浏览器**上出现，给出当前版本的安装包下载与「复制链接」。
- 地址**从站点上的 `version.json` 读**（字段 `apk`），不硬编码版本号 ——
  硬编码的话发下一版就得记得改代码，忘了就是个点不动的 404。
- 查不到（离线、这版没发布安装包）就**什么都不显示**：宁可不说，也不给一个下不动的地址。
- 刻意**不在 iOS 上出现**：iPhone 装了 `.apk` 也打不开，摆出来不是没帮上忙，是帮倒忙。
- 刻意**不在安卓壳里出现**：它自己就是那个包，让用户对着自己「下载新版」是荒谬的。

### 顺便写清了一件会让人吓一跳的事

「装到桌面」（PWA）与浏览器**是同一份存储**，数据无缝；
而 `.apk` 是**另一个独立的 App**（Origin 是 Capacitor 的 `https://localhost`），
它的 localStorage 与浏览器里那份**完全是两套**。
不写清楚，用户装完 App 会以为「记录全没了」—— 而数据其实好好地在浏览器里。
所以这条入口里明写：**先导出备份，装好 App 再导入**。

### 验证

- 冒烟 15 → **16**：新增「安卓安装包入口」检查，四条断言 ——
  安卓浏览器里**出现** / 两个按钮都落在**可视区**里 / 下载地址来自 `version.json` 的 `apk` 字段 /
  iPhone 与壳里**都不出现**。顺便留一张 `.tmp-smoke/android-apk.png` 给 review 看（先滚到那一段再截）。
  ⚠️ 喂的 `apk` 文件名故意**与版本号无关**（`9.9.9`）：文件名跟着版本号走的话，
  把地址硬编码成 `${base}/apk/yuanqi-ledger-${version}.apk` 也照样全绿，这条检查就白写了。
  ⚠️ 「在不在可视区」这条判据**测不到父容器的 overflow 裁剪** ——
  `getBoundingClientRect` 对裁剪一无所知，它只回答"在不在窗口坐标里"。别把它说成"按钮一定看得见"。
- **自证**（两轮，共四处破坏）：
  - 一次破坏三处（去掉安卓判断、去掉壳判断、地址不看 `apk` 字段自己拼）→
    冒烟报出**恰好 3 条**失败，每条都点名了对应的那一层；
  - 把「复制链接」用 `marginLeft: 2000` 推到屏幕外 → 报出 **1 条**「不在可视区里」。
  - 两次恢复后重跑都全绿（退出码 0）。
- 其余闸门照跑：eslint 0 错 0 警 / build / test 273 条 / check:data / check:nutrition /
  check:reference / check:chunks / verify-subpath —— 全绿。

---

## [1.1.0] - 2026-09-19

**两件事：应用里能知道自己该更新了，打开时有一小段品牌动画。**

### 一、应用内更新

先说清楚一件事：**网页版和安卓 App 的更新是两条完全不同的路**，这一版把两条都补上了。

| | 网页版 | 安卓 App |
|---|---|---|
| 资源在哪 | 服务器上 | **打包在安装包里** |
| 怎么更新 | 清掉离线缓存再进一次 | **下载新的安装包** |
| 按钮 | 「立即更新」 | 「下载新版」 |

- **网页版**：原来只有 Service Worker 广播这一条线索 —— 而那是**浏览器按自己的节奏**去查的，
  挂着不关的标签页可能几天都轮不到。现在多一次**版本号对比**，什么时候查由自己说了算。
- **安卓 App**：壳里的资源是打包进去的，刷新一万次也还是那一版 —— 所以必须换个安装包。
  点「下载新版」会跳到浏览器下载，点开装一次就换好了，**记录不会丢**（同一个包名与签名）。
  ⚠️ 安卓不允许一个 App 给自己静默升级（除非走应用商店的更新接口），所以这一步**必须用户点一下**，
  这不是没做完，是平台就长这样。
- **检查时机三个**：打开时、从后台回到前台、网络恢复。少一个就会出现"挂着不管就永远收不到"。
- 检查的就是站点上一个很小的 `version.json`（`npm run deploy` 会发），
  顺带把安装包放到站点的 `/apk/` 下 —— gh-pages 每次是整体强推，所以站点上永远只有当前这一版。
- ⚠️ **发布顺序变了：先 `npm run android:apk`，再 `node scripts/deploy.mjs`**，
  否则 `version.json` 里没有安装包地址（脚本会明确警告一句，不会静默漏掉）。

⚠️ 顺带修掉一个**一直存在但没人注意**的毛病：「稍后」原来记的是一个**布尔**，
意思是"永远不再提示"—— 也就是说用户点过一次「稍后」，**以后任何新版本都不会再告诉他**，
这个功能就只有第一次有效。现在记的是**版本号**，「稍后」只对那一个版本生效。
（老键为 true 的用户会被读成"当前这个版本已推迟"，下一个版本照样提示。）

### 二、启动动画

打开时先盖一层品牌动画：那个 300° 的环**自己画出来**，品牌名随后淡入，
React 一挂载就淡出（约 0.4 秒）。三点说明：

- ⚠️ **遮罩是服务端渲染在 HTML 里的**，不是客户端组件画出来的。放客户端就晚了 ——
  那时候白屏已经闪过去了，而它存在的唯一理由就是盖住"HTML 到了、React 还没水合"那段白屏。
  （冒烟里专门抓原始 HTML 来钉这一点：渲染出来的页面上看不出区别。）
- **三个出口**，缺一个都可能让装饰挡住人：正常 ~420ms 收掉；**点一下/按一下立刻收掉**；
  以及 `layout.tsx` 里 4 秒的硬兜底（万一 React 没起来 —— 一层永远盖着的遮罩比白屏更糟，
  用户会以为卡死了）。
- 形状与**安卓启动图刻意一致**：手机上是「系统启动图 → 这一层 → 应用」，不一致就会看见一次跳变。
  并且尊重 `prefers-reduced-motion`：动画被压到 0.01ms 时落在"已经画好"这一端，
  减弱动效的人看到的是完整的标记，不是一个空环。

### 验证

- 单测 262 → **273**（新增 `lib/update.test.ts`：版本号按**数字段**比而不是按字符串
  —— `1.10.0` 比 `1.9.0` 新；以及离线 / 404 / 返回 HTML / 字段不对**全都变成 null**，绝不抛）
- 冒烟 13 → **15**：
  - 启动动画：遮罩在**服务端 HTML** 里 / **3.2 秒内**收起且不可点 / 环真的有动画
  - 应用内更新：同版本**不打扰** / 网页版出现横幅且是「立即更新」/ 点「稍后」记下**版本号** /
    同版本再进不再提示 / 壳里（stub `isNativePlatform`）按钮变「下载新版」且打开的地址正确
- **自证**：一次破坏三处（遮罩不收、不比版本号、不记版本号）→ 冒烟报出 **4 条**失败。
  ⚠️ 这一轮自证还挖出**我自己的检查有问题**：「同版本不打扰」那条原本**是空转的** ——
  它靠"普通构建的 `out/` 里没有 version.json"顺带成立，把版本比较整个删掉也照样绿。
  改成自己喂一个同版本进去才有牙。同时发现样例数据里那个老布尔键会污染前置（地雷 18 的老毛病）。
- 其余闸门照跑：eslint 0 错 0 警 / build / check:data / check:nutrition /
  check:reference / check:chunks / verify-subpath —— 全绿

---

## [1.0.0] - 2026-09-19

**正式版。功能集合到这里为止，数据在你自己设备上，网页版和安卓 App 都能用。**

### 这一版意味着什么

- **功能冻结**：记录（吃 / 喝 / 动 / 睡）+ 健康参考 + 菜单库 + 周报 + AI 两处（「帮我挑」和「这个数不对？」）+ 离线 PWA + 安卓 APK。
- **数据契约稳定**：`recipe.*` 的 localStorage 键名与备份格式不再变；老备份任何时候都导得回来。
- **流程变了**：**从这一版起，任何改动先开分支**，你确认之后才 merge 回 `main`（这条规矩是你在
  `AGENTS.md` 里定的，之前一直写着"1.0.0 起生效"，现在真的生效了）。

### 发布顺序上最后修掉的一件事

发版前按你的话把「盖饭 / 家常菜」那一类过了一遍，撞见一个**差十几倍而看不出来**的错：

| 说法 | 当时算出 | 真实大概 |
|---|---|---|
| 一份番茄炒蛋 | **42 kcal**（退而记成了一个西红柿） | ~180 |
| 一份青椒肉丝 | **50 kcal**（记成青椒） | ~250 |
| 一份咖喱牛肉饭 | **34 kcal**（记成 10g 咖喱粉） | ~650 |
| 一份土豆丝盖饭 | 152 kcal（记成一个土豆） | ~600 |
| 一份鱼香肉丝饭 | 285 kcal（只算菜、**整碗米饭丢了**） | ~600 |

根因是模糊检索的最后一档「被查询包含」会挑名字最长的**原料**，而且结果还标着「估算」，
看起来完全正常。现在加了两条守卫（只在那一档生效）：

1. 命中的名字要覆盖查询的 **≥60%** 的字 —— 这条线不是新定的，`menu.ts` 里估算外卖菜品时
   用的就是它（「拆不干净就拒绝估算」）。
2. 查询以**主食尾缀**（饭 / 盖饭 / 拌饭 / 面 / 米线…）收尾时，命中的名字本身也得带那个尾缀 ——
   「鱼香肉丝饭」不能再被当成一份鱼香肉丝，那等于把整碗米饭丢掉。

触发时**不给数字**，走已有的「库里没有 X，从下面挑一个相近的 / 用搜索添加」那条路。
库里有名字的照旧一步算出来（烤肉饭 / 宫保鸡丁饭 / 鱼香肉丝 / 红烧肉 / 麻婆豆腐 / 西兰花 / 白粥
都实测还认得）。

⚠️ 代价说清楚：以前能"糊弄过去"的说法，现在会要你点一下确认。换来的是不再悄悄少算十几倍 ——
这笔交易在账本这个场景里是划算的：**一个看起来精确的错数，比一句"估不出来"有害得多。**

### 顺手修掉的

- **餐次词补进噪音表**（早饭 / 午饭 / 晚饭 / 午餐 / 晚餐…）。之前表里只有「中午」「晚上」，
  于是「午饭吃了红烧肉」整段被当成食物名去检索 —— 加了新守卫之后它会变成"认不出"，
  两处一起修才对：该剥的剥掉、单独一句「午饭」仍给「这是整餐的说法」。
  ⚠️ 剥噪音的"不许剥空"只对餐次词生效 —— 一刀切会把「我吃了」也变成一条记录（实测三条变四条）。

### 明确的已知限制（写在这里，免得以后当成"没发现"）

- **184 条数值没有可复核出处**（「通用成分值」「按标准菜谱估算」这类模糊口径）。
  按之前商定的方向，**不逐条去补**，而是让不确定性在界面上可见（档位、口径说明、估算标签、
  「这个数不对？」入口）。它们仍然不是权威数据。
- **带「+」的套餐名不救**（194 道菜单里 97 道这一类）。真正该做的是把「估不出来」的出口做顺手，
  不是硬猜。
- **河粉 / 面包糠 / 粿条**官方平台确实没有，眼下用炒米粉 / 小麦粉近似。
- **菜单库的关联适配**按你的话留到 1.0.0 之后专门做。
- **安卓 APK 没在真机上装过** —— 这台机器没有设备也没有模拟器。包里该验的都验了
  （包名 / 版本 / 签名 / 图标五档密度 / 资源齐全 / 资产按根路径），但"装到手机上真的能用"
  要你点一遍。
- 数值是**估算与参考**，不是医学建议。

### 数据与规模（1.0.0 实测）

| 项 | 数量 |
|---|---|
| 食物库 | **249** 条（按每 100g/ml） |
| 份量规则 | **142** 条（151 个档位） |
| 成品菜配方 | **22** 份（按配方估算的条目必须能重算回去） |
| 数值引用台账 | **45** 条 |
| 单测 | **262** 项 |
| 闸门 | eslint / build / test / check:data / check:nutrition / check:reference / check:chunks / verify-subpath / smoke |

### 发布

- 网页版：GitHub Pages（子路径 `/yuanqi-ledger/`）
- 安卓：`dist/yuanqi-ledger-1.0.0.apk`（已签名，`versionName` 由 `package.json` 推出来）

---

## [0.13.2] - 2026-09-19

**把你真点的外卖和食堂菜做进去了 —— 顺便挖出「整碗米饭被丢掉」这个一直在犯的错。**

### 起因是你给的两张截图 + 一句「食堂常吃咖喱鸡肉饭、宫保鸡丁饭」

先把现状跑了一遍（`npm run probe`），四个里有三个是错的：

| 说法 | 原来 | 现在 |
|---|---|---|
| 一份宫保鸡丁饭 | 记成「宫保鸡丁 150g」，**整碗米饭丢了** → 315 kcal | 400g / **604 kcal** |
| 一份咖喱鸡肉饭 | 库里没有 → **0 kcal** | 520g / **624 kcal** |
| 一份手撕鸡奥尔良烤肉拌饭 | 命中别名「烤肉拌饭」→ 300g / 387 kcal | 570g / **735 kcal** |
| 一份土豆泥 | 走分类兜底（无规则） | 190g / **201 kcal** |
| 一份猪角烤肉饭（截图里那个牌子写法） | 按普通烤肉饭算 300g / 504 kcal | 400g / **672 kcal** |

新加的料与菜（数值按配方算，配方能重算回去）：

- **咖喱粉** —— 官方平台实测，编号 **1580**（1416 kJ → 338.4 kcal/100g），已进引用台账。
  这是第一件**纤维高到改变判据**的条目，见下面。
- **土豆泥 / 咖喱鸡肉饭 / 宫保鸡丁饭 / 手撕鸡奥尔良烤肉拌饭 / 猪脚烤肉饭** —— 各自有配方，
  原料只用库里已有的条目；每条的假设都写在 `note` 里（比如奥尔良烤肉用「烤鸡腿」近似、
  孜然辣酱只按酱油+油算、钠因此偏保守）。

### ⚠️ 挖出来的三件事

1. **「更具体的规则排在泛化规则后面」＝ 那条规则永远走不到，而且看不出来。**
   份量表是同一量词下**先命中先赢**。我第一版把新规则追加在文件末尾，于是
   「手撕鸡奥尔良烤肉拌饭」被前面的 `份[…拌饭]=300g` 挡住、「猪脚烤肉饭」被 `份[…烤肉饭]=300g` 挡住 ——
   自己那条 570g / 400g 是**死的**，而 ④b（有没有规则命中）和 ④d（note 写没写）**都是绿的**，
   只有克数是错的。
   同一个病当天更早还咬过一次：`一份鸡米花` 一直是 150g，那是单字「鸡」的泛化规则抢在前面，
   专门写的 100g 从没生效过。
   **所以给闸门加了 ④b2**：同一量词下若还有**更长（更具体）的词**能命中同一条食物、
   且克数不同 —— 就报出来，并点名是哪一条被挡住。
   这条新检查一上线**当场又挖出两处既有问题**（都是之前没人看见的）：
   - **一份烤鸭饭** 命中的是「烤鸭」= 150g（那是一份鸭肉的量），**整碗米饭丢了** → 改成 330g
   - **一杯乳酸菌饮料** 因为别名「酸奶饮品」含「酸奶」，命中了酸奶的 150g → 改成 330g
2. **膳食纤维只供能约 2 kcal/g，不是 4。** 咖喱粉（纤维 36.9g/100g）一来，闭合校验就报了：
   按 4/9/4 算 415 kcal，比标注的 338.4 高 **22.6%**；把纤维按 2 算就是 341.2，差 0.8%。
   这是判据的问题不是数据的问题，所以改了闸门：**有纤维数据的条目，纤维那一部分按 2 kcal/g 计**。
   纤维低于 5g 的条目受这条影响不到 10 kcal —— 改完复跑，233 条被检查的条目**一条都没新增失败**。
3. 自证模式从**五处破坏加到六处**（新增「表头塞一条泛化拌饭规则」那条）。
   六条检查各有一个破坏点，少一条都会当场报自证失败。

### 验证

- 探针逐条复核：上表 5 道全部算对；而 `一份烤肉饭`（420 kcal）、`一份猪脚饭`、
  `一份北京烤鸭`、`一杯酸奶` 这些**原有**结果一个都没变 —— 挪动规则没有误伤
- `check:nutrition`：249 条食物 / 142 条份量规则，闭合校验 233 条，**0 问题**
- `check:reference`：22 份配方全部重算得回去，台账 45 条
- 其余闸门照跑（见下）

---

## [0.13.1] - 2026-09-19

**份量表里那些「单字词」清掉了 —— 一个蛋炒饭不再按一个鸡蛋算，一个薯片不再按一个土豆算。**

### 错在哪

份量表的 `match` 是**子串匹配**，而里面混着单字词：`蛋`、`油`、`菜`、`肉`、`鱼`、`烤`……
于是「蛋炒饭」里的「蛋」、「油条」里的「油」、「鱼香肉丝」里的「鱼」全都算命中：

| 说法 | 原来算成 | 真相 |
|---|---|---|
| 一个蛋炒饭 | **55g**（按一个鸡蛋，说明还写着「不含壳」） | 一份六百多 kcal |
| 一瓶油条 | **500g**（按一瓶植物油） | —— |
| 一条鱼香肉丝 | **300g**（按一条鱼） | —— |
| 一串北京烤鸭 | **30g**（按一串烤串） | —— |
| 一勺糖醋里脊 | **8g**（按一勺糖） | —— |
| 一个薯片 | **150g**（按一个土豆） | 一小包 70g |

更麻烦的是**它看起来完全正常**：单位、份量、说明都齐全，数字就是个错的。

### 怎么改的

**单字 `match` 词只认精确命中**（食物名或别名正好就是那个字），多字词才做子串匹配 ——
和 `searchFoods` 拒收单字名字是同一条理由（见 `AGENTS.md` 地雷 20）。

判断依据是先量了一遍，不是拍脑袋：这类单字命中一共 **100 处**，
其中 **68 处**给出的克数和「按分类兜底」**一模一样** —— 它没带来任何信息，只带来了错的那部分。

然后按量出来的结果逐条改数据：把靠单字泛化的规则换成**显式列名**
（`份[肉,鸡,鱼,牛,猪…]` → `份[红烧肉,回锅肉,宫保鸡丁,鱼香肉丝,…]`），
并补上失去覆盖的 37 条。⚠️ **该说「估不出来」的地方现在会明说** ——
上面那张表里的说法，现在会走分类兜底并在界面上标「估算」，而不是给一个自信的错数。

### ⚠️ 顺带挖出的两件事

1. **闸门的判据和运行时漂移了，而且漂得很难看。** 闸门是纯 JS，自己抄了四份匹配逻辑；
   运行时改了判据之后它没跟着改 —— 于是「3 条死规则 + 37 条失去覆盖的食物」
   它一个都没报、**整个闸门照样全绿**。一条不再反映现实的闸门比没有闸门更糟。
   现在闸门里只剩一份镜像判据，并加了一条**只有精确匹配才拦得住**的自证：
   往一条活规则里塞个单字词「饺」。判据要是退回子串，这次自证会当场失败
   （实测过：退出码 2，并且点名是哪条检查没拦下）。
2. **「逐条规则」查死词把死词藏住了。** 一条规则里混着 3 个活词 + 1 个死词时，死词永远看不见。
   改成**逐词**查之后一次报出 8 处，其中 6 处是一直躺在那儿的：
   `盖饭`、`盖浇饭`、`水果`、`蘑菇`、`香菜`、`芹菜`、`茼蒿` —— 库里根本没有这些食物名。

### 还有一条被纠正的旧断言

`一份鸡米花` 原来是 **150g**，靠的是单字「鸡」的泛化规则**抢在**专门规则前面；
而库里专门给它写的那条（100g）**一直是死的**（先命中先赢）。
单字词清掉之后专门规则才真正生效，测试里那条断言也跟着从 150 改成 100 ——
**改的是错的那一侧**，这是这次唯一的"数字变了"的既有行为。

### 验证

- 单测 248 → **254**（新增一组「单字词只认精确命中」，钉的是真实数据 + 真实运行时）
- `check:nutrition` 新增**逐词死词**检查，自证从四处破坏加到**五处**
- 逐条探针复核（`npm run probe`）：上表 7 种说法全部不再给出错数，
  而「一个鸡蛋 / 一个梨 / 一只虾 / 一勺盐」这些**正确**的精确命中**没有受影响**
- 其余闸门照跑：eslint 0 错 0 警 / build / check:data / check:reference /
  check:chunks / verify-subpath / smoke 全绿

---

## [0.13.0] - 2026-09-19

**有了一个能装到安卓手机上的 App（APK），顺手修掉一个图标偏心 26.8% 的老 bug。**

### 安卓 App（Capacitor 壳）

Web 那一半本来就是完整的 PWA，所以这里不加任何功能，只做一层**壳**：
用 Capacitor 把静态产物（`out/`）装进 WebView，`appId = com.orang1ver.yuanqiledger`，
桌面名字「元气账本」，`versionName` **直接读 `package.json`**（不再手写第二个版本号）。

⚠️ 三件刻意的做法：

1. **打包时构建不带 `BASE_PATH`**。子路径是给 GitHub Pages 的（`/yuanqi-ledger/`），
   而 WebView 从 `https://localhost` 起、站点根就是 `/` —— 带上子路径的表现是
   **页面能开、`_next/` 全 404，也就是纯白屏**。`build-apk.ps1` 因此会**断言产物里没有子路径前缀**。
2. **签名密钥不进仓库**（`.gitignore` 里把 `*.jks` / `keystore.properties` 打开）：
   仓库是公开的，密钥进了 git 就永远换不掉。密钥在 `D:\Android\keystore\`，
   只由 `android/keystore.properties` 指过去。**没有它 Gradle 照样"成功"**，
   只是产出一个装不上的未签名包 —— 所以脚本先断言密钥在，再用 `apksigner` 验一遍。
3. **`sw.js` 的版本占位符要在打包前注入**（与 `deploy.mjs` 同一套替换）。
   不做这一步，APK 里的缓存名永远是字面量 `__VERSION__`，装上新版之后旧缓存还在 ——
   正是 iOS 上踩过的「看不到新版」那一类。

**这台机器的网络坑**（都写进 `scripts/android/setup-sdk.ps1` 的注释了）：
`dl.google.com` 直连不通（本机没开代理），所以 SDK 是**从腾讯镜像手工铺目录**装的，
`maven.google.com` / Maven Central 换成阿里云镜像。
镜像上没有 build-tools 35，这一条**反过来决定了整个版本矩阵**：
`AGP 8.6.1 + Gradle 8.11.1 + compileSdk 35 + build-tools 34.0.0`（AGP 8.7+ 硬要 35）。
Capacitor 自己的两个子模块硬编码 AGP 8.7.2 且从 `google()` 拉，
根工程改不了 `node_modules`，于是在 `android/build.gradle` 里为所有子工程**前置镜像 + 把 AGP 压到 8.6.1**。

### 顺带修掉：maskable 图标的环偏在左上角（画布的 26.8%）

`make-icons.py` 的 `maskable_logo` 把 `w * 0.62` 当成 `size` 传给了 `mark_color`，
而 `mark_color` 是**以 `size/2` 为中心**的 —— 于是环连同中心一起被挪到了左上角。
实测（读 git 里那一版 PNG 算外接框）：框中心 `(158.5,158.5)`，画布中心 `(255.5,255.5)`，
**偏 137.2px**。maskable 图标在启动器里会被裁成圆形，表现就是「环偏在一角」。
改法是给 `mark_color` 加 `scale`（只缩半径，不动中心），并把启动图/自适应图标都走同一条路。

⚠️ 这条不可能靠肉眼发现，所以加了一条**自证过的**断言：出图前先算环的外接框中心，
偏移超过 1.5px 就报错。判据用**外接框**而不是像素重心 —— 环是 300° 的弧，
缺的那 60° 本来就会把重心拉偏（实测 18.6px），用重心会误报。

### 验证

- **APK**：`aapt2 dump badging` → `versionName='0.13.0'`（与 `package.json` 一致）、
  `sdkVersion 23 / targetSdkVersion 35`、`application-label: '元气账本'`、
  `launchable-activity: com.orang1ver.yuanqiledger.MainActivity`；`apksigner verify` 通过
  （SHA-256 `cf9beecb…`）。3.5MB。
- **图标自证**：把「用小画布」那个错法塞回去 → 断言报出
  「外接框中心 (158.5,158.5)，画布中心 (255.5,255.5)，偏移 137.2px」，
  与 git 里那份旧图标实测的偏移**完全一致**。
- **Web 那一半没受影响**：全部闸门照跑（见下）。

---

## [0.12.0] - 2026-09-19

**不再假装每个数字都准 —— 把「这一笔是怎么算出来的」摆到桌面上：能当场改，也能当场质疑。**

原来的处境是：库里对同一道菜只有**一个**数。而红烧肉有肥有瘦、蛋炒饭有少油有多油、
饺子有大有小 —— 差别能到一倍以上。给整库「校准数值」做不到（也没有出处），
所以这版换个方向：**让不确定性可见**。

### 一、份量档位（第一块：数据与解析）

- 份量表「个 / 只 + 饺子」原来只有一档 20g → **小 15 / 中 20 / 大 30**（「只」同口径）
- 说话里带尺寸词也认了：「两个**大**饺子」原来按中号算 —— 而界面上看不出任何异常
  （单位、份量、说明都齐全，就是小了一号）。现在在 `parseFragment` 的**出口**剥尺寸词。
  ⚠️ 刻意包一层，而不是在内部 5 个 return 点上各剥一次 —— 漏一个就会在某种写法下静默失效。
- ⚠️ 过程中撞出一个真 bug：「**大白菜**」是库里的正名，被剥成了「白菜」。
  判据必须是**精确**匹配（`findFoodByName`）而不是 `matchFood` ——
  「大饺子」也能被 `matchFood` **模糊**配到「饺子」，用它会让尺寸档永远不生效。

### 二、做法变体（同一道菜多条）

红烧肉 460/45/800（source 写着「按标准菜谱估算」，没有出处）→ 三条**按配方算**的：
瘦 255 / 正常 345 / 肥 386；蛋炒饭 180/6/650 → 少油 132 / 正常 144 / 多油 167。

`SUGAR_SYNONYMS` → `VARIANT_SYNONYMS`：从只认糖度扩到肥瘦、油多油少。
只在**同一主名的兄弟**之间挑，所以「少油」不会把奶茶挑成蛋炒饭。

⚠️ 第一版配方**漏了「盐」**，蛋炒饭的钠只有 114（原值 650）—— 补上才对。
补完之后红烧肉的钠 801/798/739 与原值 800 几乎一致，说明原值的钠本来是对的。

### 三、已落库的那条记录：档位能换（界面）

以前只能删了重记。现在每条记录下面写明**当时按哪一档算的**，并给一排 chips 直接换：
「按『中』算的 —— 这个量词下有 3 档，点一下就能换」。

- ⚠️ 档位**不写进 `DietEntry`**：那是持久化契约，加字段意味着所有**老记录**都没有它 ——
  而老记录恰恰是最需要被解释的那些（用户觉得数字不对，多半是回头看旧账）。
  档位本来就能**反推**（`grams ÷ amount` 与份量表比），改了克数也照样反映得出来。
- ⚠️ 反推不出来时返回 `null`，**绝不编一个默认档**：
  说「按中号算的」而实际不是，比什么都不说更糟。
- 换档改的是**落库的克数**，营养快照跟着重算（走 `editDietEntry`）。

### 四、「这个数不对？」—— 反馈回路

每条记录旁边一个入口，用**自己的话**写一句质疑（不填表）。然后：

- **有 Key**：交给模型做**归因** —— 这个数能不能用已有信息解释清楚
  （份量怎么折算的、数值来源是什么、口径写没写）。
- **没 Key**：直接跳到给反馈文本。少了这条路，没配 Key 的用户连「这个数不对」都说不出来。
- 解释不了时，整理出一段**能直接贴到 GitHub issue 的文本**，旁边「复制这段」+ 链接。

⚠️ 三条刻意的地方：

1. **模型一个营养数字都不产生**（与「帮我挑」同一条红线）。它只说「可能」「看起来」，
   数值一律由本地算。屏幕上所有数字都是算出来的。
2. **没有服务端，所以没有「上传反馈」这条路。** 能做的是把上下文整理成人能贴的一段话，
   由用户自己发过去。与其放一个假装能上传的按钮，不如把这段文本写清楚。
3. **脱敏从类型上定死**：`FeedbackContext` 里**没有**健康档案、**没有**其它记录，
   只有这一笔的事实。用户一点复制就贴到公开 issue 上了 —— 这里漏一点就是真漏。
   ⚠️ 模型建议的档位走**白名单**（只在真实存在的档位里收），
   否则界面上会多出一个**点了没反应**的按钮 —— 那种坏法比不给建议更让人困惑。

### 验证

- 单测 230 → **248**（新增 `lib/nutrition/tiers.test.ts`、`lib/ai/feedback.test.ts`）
- 冒烟新增 `checkEntryFeedback`，9 条断言：档位行只出现在多档食物上 / 点档位后
  **落库克数**与**热量快照**都对得上 / 无 Key 时按钮文案是「生成反馈文本」/
  反馈文本里有饺子·15 个·用户的疑问·issue 地址 / 点「复制这段」不会静默无反应
  （⚠️ 刻意**不去读剪贴板内容** —— 无头浏览器没焦点时 `writeText` 本来就常被拒，
  断言真内容会让检查随机变红；要钉的是「不会静默什么都不做」）
- **自证**：同时做两处破坏 ——
  ① 让那段文本顺带带上健康档案、② 换档时把「每单位克数」当成总克数写回 ——
  冒烟报出**7 条**失败：4 条「反馈文本里漏出了健康档案里的过敏项／身体状况／身高／体重」、
  2 条「落库克数是 30，期望 450」「热量快照没跟着重算」、
  1 条「换了档位，说明文字还写着原来那一档」，退出码 1。
  那两样（一条别的记录 + 一份健康档案）是**特意埋在页面里**的：
  只断言「文本里有饺子」的话，一个把整页 `innerText` 当反馈文本的实现照样全绿。

---

## [0.11.0] - 2026-09-19

**安卓用户终于知道这东西能装到桌面了。**

### 安卓「装到桌面」提示（补上一个真缺口）

原来只有 iOS 有安装引导，安卓**什么都没有** —— 而 `IOSInstallHint` 的注释里还写着
「Android/桌面 Chrome 会自己弹安装提示」，**那句话早就过期了**：

**Chrome 从 76 起移除了 mini-infobar。** `beforeinstallprompt` 仍然会触发，
但它**自己不显示任何 UI** —— 只给你一个 `prompt()` 的能力。
也就是说「什么时候问、怎么问」必须应用**自己做，不做就等于没有**：
安卓用户只会看到一个地址栏，根本不知道这东西能装到桌面。

现在：

- 新组件 `AndroidInstallHint`：收到「可安装」信号 → 顶部一条提示 + 「安装」/「不用了」
- 点「安装」**真的调起系统安装流程**（`prompt()`），不是个摆设按钮
- 点「不用了」**永久关闭**（新键 `recipe.androidInstallHintDismissed.v1`）
- 已经装在桌面上了（`display-mode: standalone`）就不再提 —— 与 iOS 同一个判据

⚠️ 两个刻意的做法：

1. **必须 `preventDefault()`** —— 拦下来我们才有机会问；不拦的话浏览器什么都不做，
   这个组件等于白写。
2. **`DismissibleKey` 用 union 而不是 `StorageKey`** —— 「关掉就不再显示」的那几个键单独成组，
   免得哪天顺手把饮食记录当开关写坏了。

### 顺带

- `manifest.json` 补了 `categories`
- **iOS 那边复核过、已经完整**（图标 192/512/maskable + 4 个启动图尺寸 + standalone + 更新机制），不用动
- ⚠️ **打安卓 APK 是另一件事，这版没做**：这台机器缺 JDK 与 Android SDK
  （`adb` 是 WinGet 单独装的 platform-tools，它上级目录里没有 platforms/build-tools）。
  要装约 4~6GB，**且约定一律装到 D 盘**（系统盘空间紧）。计划是先让 PWA 这半稳下来再动它。

### 验证

冒烟新增 `checkAndroidInstallHint`，四条断言（可安装信号→出现 / 点安装**真的调 prompt()** /
点不用了永久关闭 / 重载后不再出现）。

**自证**：去掉 `prompt()` 与把 `dismiss` 写成别的键两处破坏 → 冒烟报出
「点了「安装」却没调 prompt() —— 那按钮是个摆设」等三条，退出码 1。
⚠️ 第 2 条断言是特意加的：**就算 `onClick` 里漏掉 `prompt()`，引导条照样会消失** ——
不单独钉它，这条检查会假绿。

---

## [0.10.0] - 2026-09-19

**补上了库里最基础的缺口（面粉、淀粉……），菜单库能算的从 31 道到 39 道。**

### P6.1 第二批（续）：5 条原料 + 5 道成品菜

上一批**卡在原料上**：裹粉要面粉、猪脚饭要猪蹄、烤鸭饭要烤鸭，库里一条都没有。
所以这批先补原料，再按配方做菜。

| 新原料 | kcal/100g | 官方编号 |
|---|---|---|
| 小麦面粉（标准粉） | 353.3 | 259 |
| 玉米淀粉 | 350.9 | 320 |
| 猪五花肉 | 343.5 | 785 |
| 猪蹄（熟） | 259.3 | 810 |
| 北京烤鸭 | 431.4 | 901 |

| 新成品菜 | kcal/100g | 一份 |
|---|---|---|
| 炸鸡排 | 305.5 | 120g |
| 烤冷面 | 284.1 | 163g |
| 小笼包 | 267.0 | 200g |
| 烤鸭饭 | 194.6 | 313g |
| 猪脚饭 | 155.3 | 355g |

**炒河粉不单独建条目** —— 它和炒米粉的原料与比例几乎一样（大米制品 + 蛋 + 肠 + 油），
分开建两条只会让两份几乎相同的数字漂移。改成给「炒米粉」补别名（炒河粉 / 炒粿条）。

**覆盖率**（那份 194 道的菜单库）：**31 → 39 道（16% → 20%）**，其中单品 20 → 27。
这一批新认出来的包括：炸鸡排饭、三条烤冷面、烤鸭饭、正宗隆江猪脚饭、片皮鸭中卷、
三条炒河粉/炒米粉、小笼包。

### 台账那处说法是错的，已修正

`data/foodSources.json` 原来写着「要核对就拿着 `page` 回官网按编号查」——
**但官网 UI 上没有「按编号查」的入口**。那个数字是查询接口里的**食物编号**。

证据很硬：台账里 `zhuliji`（猪里脊）记的是 **784**，接口返回的 code 784 = 猪肉(里脊)
= 154.9 kcal，而库里存的是 **155** —— 完全吻合。

- 字段改名 `page` → `code`（39 条）；source 描述「（网页 XXX）」→「（编号 XXX）」（39 处）
- `meta.note` 写清**怎么核对**：POST 哪个接口、字段叫什么、返回里第几个是 code / 能量 / 钠
- 新增 `scripts/fetch-food-table.mjs` —— **取数工具**（要联网，所以**不进**闸门链）。
  有了它，「每条数值都可复核」才有一条**走得通**的路；否则台账里那个数字只是个没人能验证的记号。
  ⚠️ 一个坑写进了工具说明：按名字搜时**别带括号**（搜「猪肉(里脊)」搜不到，得用「里脊」）。

### 闸门抓出来的两件事

**① 别名撞车。** 0.8.0 我给「肉包」加过「小笼包」、给「炸鸡」加过「炸鸡排」——
当时库里没有专门条目，靠复用数值让它们能算，那是对的。
现在补了更准的专门条目，两边就撞上了。`check:nutrition` **精确报了出来**
（「小笼包」已经被 baozi-pork 占用），已让别名让位。

**② 两条新原料被「不相关的规则」覆盖。**
**玉米淀粉**命中了 `个[玉米]`（因为名字里含「玉米」），
**北京烤鸭**命中了 `串[烤]`（那是**单字 match**，任何含「烤」的食物都会踩上）。
闸门 ④b 只看「有没有命中」，所以被骗了过去 —— 已给它们加明确规则。

> 顺带记一个**闸门的已知弱点**：④b 分不出「命中的是食物类别词（`份[肉,猪]`，命中合理）」
> 还是「做法词（`串[烤]`，命中很宽）」。前者是设计意图，后者是覆盖面过宽。
> 要自动分辨得给规则加标注，暂时没做 —— 所以加新食物时**必须自己看一眼**它命中了哪条规则。

---

## [0.9.0] - 2026-09-19

**导入这条路补上了安全网；顺带修掉一处用户能看见的星号。**

### P9.2：导入预览重做（原来是个数据安全隐患）

原来的确认是**嵌套三层 `window.confirm`**：

```js
confirm("点「确定」= 覆盖导入…") ? "overwrite"
  : confirm("要合并导入吗…") ? "merge" : null;
```

按钮上只有「确定 / 取消」，而**「覆盖」是整份替换** —— 没有快照、没有撤销，
误点一次数据就没了。这跟项目自己的红线（破坏性操作前先存档、给一次撤销）是矛盾的。

现在换成一张**导入预览面板**：

- 先列出**这份备份里有什么**（每一类几项，而不是一句拼起来的话）+ 导出时间 + 是否含 AI Key；
- 两个按钮**各自写明后果**：「合并导入（只补本地没有的）」/「整份覆盖（替换现在全部数据）」；
- 「整份覆盖」**就地再确认一次**（不用 `window.confirm` —— 那又回到原点了）；
- 覆盖**之前自动存快照**，导入完可以在设置页点「撤销这次导入」，**只能撤一次**。

新键 `recipe.importUndo.v1`：单槽快照，与 `takeoutUndo` 同一个设计。

⚠️ 两处刻意的取舍：

1. **快照写不进去（配额满）时不拦住导入，但也不承诺能撤** ——
   那时界面上那个撤销按钮根本不会出现。不骗用户，也不让他卡死。
2. **覆盖后 1.2 秒自动重载**：内存里的 hook 还拿着导入**之前**的数据，
   用户接着打卡就会把旧值写回去、冲掉刚导入的东西。快照已经在 localStorage 里，
   重载后撤销按钮自动出现，所以有退路。

### P9.3：文案表 —— 只抽「必须一致」的那几处

原计划是「抽 `lib/copy.ts` 集中界面文案」。**探索后把范围缩小了**：
`app/` 下 `.tsx` 有 698 处含中文的行（绝大多数是注释），全量抽取会得到
几百行 copy.ts + 几百处改动，**对用户没有可见收益**，还让组件离自己的文案更远
（与 AGENTS 地雷 7「页面要一眼读完」的方向相反）。

新建的 `lib/copy.ts` 只放三类**一旦不一致就是错的**：

- `PAGE_ROUTES` / `PAGE_LABELS`：同一个页面在导航、页头、提示语里必须同名；
- `DISCLAIMER`：免责声明；
- `HEALTHY_RANGE_NOTE`：健康体重区间是**参考**、不是给用户定的目标。

**顺带修掉一个用户能看见的瑕疵**：周报页那句里的 `**参考区间**` 是 JSX 文本节点，
**屏幕上真的印着星号**。现在收进常量、去掉星号，单测钉住「共享文案里不许有 `**`」。

**统一了页面名**：底部导航原来叫「菜单」、页头叫「菜单库」，
而首页提示语让用户「去「菜单」关联一下」—— 他照着去找一个叫「菜单库」的页面。
现在都取页头那个更完整的名字。

### 顺手删掉的死代码

`describeBackup` 与 `looksLikeLegacyBackup` —— 预览面板改用结构化的
`describeBackupDetail` 之后，两个都没有调用点了（后者连测试都没有）。

### 验证

单测 205 → **226**（新增 `lib/storage/backup.test.ts` 14 条、`lib/copy.test.ts` 7 条）。
冒烟新增 `checkImportPreview`，四条断言并**逐条自证会失败**：

- 把预览的条数算错 → 报「面板没数出饮食日记的 2 条」；
- 去掉二次确认 → 报「「整份覆盖」没有二次确认」；
- 覆盖前不存快照 → 报「没有出现「撤销这次导入」」。

三条各自被**对应**的断言抓到，退出码 1。

---

## [0.8.0] - 2026-09-19

**拿你真实的那份菜单库（194 道）做了一轮：能算的从 18 道变成 31 道。**

### P6.1 第二批：6 道成品菜

数字不是估出来的，是**照配方加出来的** —— 每一项原料都回库里查表，可逐条复核。

| 新条目 | kcal/100g | 一份多重 |
|---|---|---|
| 麻辣烫 | 82 | 500g（含汤水） |
| 卤肉饭 | 145 | 285g |
| 烤肉饭 | 140 | 315g |
| 重庆小面 | 131 | 310g |
| 炒米粉 | 139 | 345g |
| 甘梅地瓜条 | 225 | 155g |

另外给库里**语义等同**的条目补了别名，数值复用已有的、没有新编一个数：
小笼包 / 汤包 → 肉包、炸鸡排 / 鸡排 / 琵琶腿 → 炸鸡、热狗肠 / 烤肠 → 火腿肠、
水磨年糕 → 年糕。

**覆盖率实测**（那份 194 道的菜单库）：

| | 加之前 | 加之后 |
|---|---|---|
| 能估 | 18（9%） | **31（16%）** |
| 其中单品（名字不带「+」） | 10 | 20 |
| 套餐（带「+」） | 8 | 11 |

⚠️ **带「+」的套餐仍然是没救的那一半**（97 道里只多认了 3 道）——
它们是外卖平台的商品名，不是食材描述。这条路本来的结论没变：
把「估不出来」的出口（一键关联）做顺手，比继续堆商品名划算。

### 新文件 `data/foodRecipes.json`：配方进仓库

「按配方估算」这个 source 本来最容易变成「没人说得清怎么来的」——
写个差不多的数、标一句「估算」就完了。所以配方**落盘**，
`check:reference` 拿它**重新加一遍**，与库里的值逐项比对
（热量容差 1、三大营养素 0.15、钠 1）。改一个克数就当场对不上。

⚠️ 它保证的是「算得对」，**保证不了「配方就是这家店的做法」**，脚本会把这条边界打印出来。

### 闸门一上线就抓出的 4 条历史遗留

**珍珠 / 拿铁 / 卡布奇诺 / 摩卡** 标着「按配方估算」，**却从来没有配方** ——
那个说法会让读者以为它有依据。已改成明确标为模糊口径（`通用成分值（…推算）`），
它们会正确地进待核清单。

### P9 性能复查：新闸门 `npm run check:chunks`

食物库那份 JSON 约 60KB，**只有饮食页 / 首页 / 菜单库该有** ——
健康小屋与周报不查库（周报的质量分只依赖记录里的营养快照）。
这类错误写代码时完全看不出来（一行 `import` 而已），要到构建产物里翻 chunk 才会发现。

新闸门按探针词 `noodles-dry` 找含食物库的 chunk，再比每个页面引用了哪些 js。
**自证做了两遍**：

- 内存里伪造「weekly 引用了食物库」→ 判定逻辑报错；
- **真实破坏**：给 weekly 加一行 import、重新构建 → weekly 的 js 从 8 个变 9 个、
  白涨 64KB，闸门红、退出码 1；撤销后重新构建 → 恢复绿。

### 现状

食物库 **229** 条 · 份量规则 **118** 条 · 配方 **6** 份 · 台账登记 **39** 条。
单测 205 条不变（这一批改的是数据与闸门，没动营养层逻辑）。

---

## [0.7.3] - 2026-09-18

**把「一碗到底是多少」写清楚了，并且不再允许它含糊。**

### 补强：4 条份量规则写明了口径

| 规则 | 克数 | 补的说明 |
|---|---|---|
| `碗[粥,汤]` | 300g | 指煮好盛出来的重量；下锅前的生米只有四五十克 |
| `碗[饺子,馄饨,云吞]` | 250g | 指煮好沥干后的重量，约 12 个中等饺子 |
| `份[饺子,馄饨,云吞]` | 200g | 指煮好沥干后的重量，约 10 个中等饺子 |
| `盘[时蔬,青菜,菜,…]` | 250g | 餐馆一盘菜的量，按可食部分算 |

**只加了一句 note，一个克数都没动**（diff 是 4 行，每行只多一个字段）。

### 新增闸门 ④d：生熟有歧义就必须写明

带做法口径的食物（`source` 带「煮 / 蒸 / 炒 / 油炸 / …」），
命中 `碗 / 份 / 盘` 时，生效规则的默认档必须有 `note`。

判据刻意与运行时一致：按「食物 × 量词」找那条**真正生效**的规则（先命中先赢），
而不是"哪条规则提到了它"。

⚠️ 这一点是写检查时踩出来的：`碗[粥,汤]` 的 `match` 里有个单字「汤」，
「面条」的别名「清汤面」也算命中它 —— 但面条规则排在它前面，那条粥汤规则
**永远不会被用到**。按"哪条规则提到了它"去查，会给一条死规则写说明，而真正的缺口被漏掉。

自证：抹掉「一碗粥」的 note 后当场报错，`check:nutrition` 现在四处破坏各对应一条检查。

### 一个顺带看清的事实

按「规则」算，缺口只有 **4 条**；而 `docs/REFERENCE-CHECKLIST.md` 里原先估的是「约 40 条」、
扫出来的是「95 条」—— 那 95 是**食物条数**（一条规则被十几条食物命中就重复计数），
不是缺口的数量。这次把口径统一成「真正生效的规则」。

### 还没解决的

- **数值本身**（饺子钠 620、馄饨 700 是不是偏高）仍无出处，要等 P6.1 第二批的配方估算。
  这次动的是**口径**，不是数值。
- 「饺子」靠别名「猪肉白菜**菜**」命中了 `盘` 规则里那个单字「**菜**」——
  眼下克数恰好合理（一盘饺子 250g），但这是**巧合**：删掉那个别名，
  「一盘饺子」就会掉到分类兜底。这是单字 match 的老问题（AGENTS 地雷 20 的同类）。

---

## [0.7.2] - 2026-09-18

**开始提醒你备份了 —— 数据只在这台设备上。**

导出/导入能力早就写好了，缺的只是**没人提醒他去点**。
而数据全在 localStorage 里：清一次缓存、换台机器、误点一次「清空数据」，
几年的记录就没了，**找不回来**。

### 新增

- **久未备份提醒**：超过 30 天没导出备份时，顶部出现一条横幅，
  一句话说清风险（「清一次缓存就全没了」）+ 一个「导出备份」按钮。
  设置页的备份区常驻显示状态（「上次备份：N 天前」/「还没有导出过备份」）。
- 新键 `recipe.backupReminder.v1`：同时记「第一次打开」「上次导出」「静默期」三件事。

### 三条刻意的克制

1. **空库不提醒。** 还没记过东西的人，备份出来是个空文件 ——
   提醒他不但没意义，还会让「提醒」这件事在用户眼里贬值。
   判据是「有没有**用户主动记的**东西」：菜单库的示例种子不算
   （新用户一进去就已经有示例菜了），AI Key / 偏好这类配置也不算。
2. **「稍后」是安静 7 天，不是永久关闭。** 永久关闭等于这个功能不存在，
   而它是唯一能防止数据不可逆丢失的机制。
3. **横幅上的导出按钮不含 AI Key**（与设置页的默认勾选一致）。
   要连 Key 一起导出，去设置页勾 —— 默认不导出更安全。

### 为什么「刚备份过」记在 `downloadBackup` 里面

标记写在 `lib/storage/backup.ts` 的 `downloadBackup()` **内部**，而不是它的调用点。
挂到 UI 层的话，将来多一个导出入口就会漏记 ——
漏记的后果是「用户明明备份过却一直被提醒」，他会开始无视这个提醒。

### 验证

- 单测 184 → **205**：新增 `lib/storage/backupReminder.test.ts`，压的全是**误报**边界
  （空库 / 只有种子数据 / 从没备份 / 刚备份 / 静默期 / 时间倒流）。
- 冒烟新增 `checkBackupReminder`（四处断言），并**逐条自证会失败**：
  去掉天数判断 → 报「刚打开就在提醒备份」；去掉备份标记 → 报「上次备份时间没落库」。
  定位用 `[data-yq="backup-reminder"]` 而不是按钮文案 ——
  设置页里也有一个「导出备份」，靠文本子串定位迟早被别处的文案救活（地雷 24）。

---

## [0.7.1] - 2026-09-18

**修掉一处静默算错：干面条被按熟面的克数算，高估了三倍。**

### 修复

- **「一碗挂面」不再算成 865 kcal。** 挂面是**干重**数据（346 kcal/100g），
  而份量表里 `碗[面条] = 250g` 是**熟重** —— 挂面的别名「干面条」含「面条」，
  于是命中了那条熟重规则。同一个错在库里躺着**三条**：挂面 865 kcal、粉丝（干）845 kcal、
  米粉（干）同路径，真实都在 280 上下，**高估三倍**，而界面上看不出任何异常
  （单位、份量、来源一样齐全，只有数字是错的）。
  修法是给干重食物一条**按干重给克数**的专门规则（一碗 = 80g 干面），
  并把它排在通用规则**前面** —— 份量表是先命中先赢，位置比内容更容易出错。
- **「下了一碗挂面」不再丢份量。** `LEAD_NOISE` 里没有「下了 / 煮了」，
  整句被当成食物名去模糊检索：食物配到了，份量却丢了，
  退化成按分类兜底的 200g（692 kcal）。同一批补上「下了 / 煮了 / 做了 / 炒了 / 煎了 / 烤了」。
  ⚠️ 只加动词不加量词 —— 「下了个蛋」的「个」必须留给 `FRAGMENT_RE`（地雷 19）。

### 新增闸门

- **`check:nutrition` 多了「干重食物不许用熟重的克数」。**
  干重食物（`source` 标了「干重」或名字带「（干）」）的每条命中规则，
  其默认档的 `note` 必须写明是干重口径。
  判据刻意选**口径**而不是克数大小 —— 80g 和 250g 谁对，光看数字判不出来，只有 note 知道。
  原先那条「每条食物都要有份量规则命中」抓不到它：那条只问有没有命中，不问命中的口径对不对，
  **命中得越"成功"，错得越彻底**。

自证：抽掉碗的干重规则后，闸门当场报出「865 kcal」，
单测同时报「挂面 应当按干重给 80g，实际 250g」。单测 180 → 184。

---

## [0.7.0] - 2026-09-18

**周报开始谈睡眠、心情和体重趋势了。**

这三样用户一直在记（打卡卡里能填睡眠和心情，体重要称），但周报页**一个都没用**：
只算了喝水和步数的达标率，体重也只报一句「与上次相比 ±X kg」。
数据早就齐了，缺的只是有人去看它。

### 新增

- **周报多了「睡眠与心情」一张卡**：这一周平均睡几小时（几晚参与写明）、
  其中几晚到 7 小时、心情三档各几天。
  ⚠️ 7 小时写的是**参考线**，不是判定对错的尺子 —— 有人天生睡 6 小时就够。
- **一句观察性对照**：「这周记了 N 天运动，其中 M 天也记了状态不错」。
  ⚠️ 刻意**只做同期计数**：不写「运动让你心情变好」，**也不做相关系数** ——
  一周 7 个点算出来的相关系数没有意义，而它长得像个科学结论，比不给更糟。
- **体重卡补了「本周均值」与「距健康体重区间」**：区间按 BMI 18.5~23.9 由身高算出，
  **复用** `health.ts` 已有的 `healthyWeightRange`，不新造字段、不另立口径。
  文案明说这是**参考区间、不是给你定的目标** —— 增肌期的人和减脂的人，方向是反的。

### 两条贯穿这里的规矩

- **「没有记录」不是 0。** 一晚都没记睡眠时 `avgSleep` 是 `null`，
  页面说「这周没记」而不是「平均睡 0 小时」；一条体重都没有时周均是 `null`，不是 0。
  两处都有单测钉着，冒烟也专门验了一遍。
- **不做因果。** 跨维度只给计数 + 免责句，不给结论。

单测 163 → 180（新增 `lib/wellness.test.ts` 与 `lib/weight.test.ts`）。

---

## [0.6.2] - 2026-09-18

**「记一笔」解析出来的克数，现在点一下就改。**

以前想说"其实是大包"，只能自己在克数框里打 135。而这个能力**早就写好了** ——
搜索添加流程里那排「小包 / 一包 / 大包」的档位点选一直能用，只是没接到这条路上。

- `PortionChips` 抽成一个**共用组件**（原本只长在 `PortionPicker` 里）。抄两份的话两边
  迟早长得不一样，而"点档位改份量"恰恰是最常做的动作 —— 一处变了另一处没变会被当成坏了。
- 「记一笔」解析出的每条记录，克数旁出现同一排档位；点一下克数就换成该档位的克数。
- ⚠️ 点档位**只改克数、不动量词**。量词是解析出来的（「包」），而 `portionCount` 要靠它
  把当前克数反算成「≈ N 个」；把量词换成档位标签（「大包」）会让读数变成「≈ 1.9 大包」。
- 删掉死代码 `portionOptions(food, unit)`（一个调用点都没有），只留 `defaultPortionOptions`
  一个口径 —— 两套算法管同一件事，只会让人不知道该信哪个。

冒烟新增一条定向检查：**先读默认克数、再刻意点一个不同的档位**，断言界面与落库**都**变了
（一包 70g → 大包 135g）。不这么做的话，`onPick` 压根没接上时这检查照样全绿。
破坏 `onPick` 后确实变红，已自证。

---

## [0.6.1] - 2026-09-18

**又补了 13 条外卖快餐与小吃，都是「记一笔」里说得出口的东西。**

把「今天吃什么呀」的菜单库（194 道菜、82 个商家）拿来对了一遍，想看看真实在点的东西
能不能算出热量。结果是**一件事能修、一件事修不了**：

- **能修**：库里一条快餐/小吃都没有 —— 说「一个汉堡」「一份鸡米花」「一碗热干面」全认不出来。
  而官方成分表里**本来就有**这一整段（肯德基的汉堡/鸡块/鸡米花/鸡翅、年糕、豆腐脑、
  凉粉、凉面、热干面、比萨），直接取数就行。
- **修不了**：菜单库的菜名是外卖平台的**商品名**（「香辣鸡腿中国汉堡+塔塔鸡块(3块)+翅根+冰柠可乐」），
  不是食材描述。加完这 13 条，194 道菜的覆盖率只从 8 道变成 11 道（4% → 6%）。
  **不继续往里堆商品名** —— 模拟显示要覆盖它们得逐条加 240 多个别名，
  而那些名字是「这家店这次的活动名」，换一家就全变了，维护不起。

- 新增 13 条：鸡肉汉堡、鸡柳汉堡、鸡肉卷、上校鸡块、辣鸡翅、新奥尔良鸡翅、劲爆鸡米花、
  年糕、豆腐脑、凉粉、凉面、热干面、比萨饼。食物库 210 → **223** 条，份量规则 103 → **111** 条。
- 给「鸡肉汉堡」挂了别名「中国汉堡」「香辣鸡腿堡」等 —— 这些词在外卖菜名里出现最多。
- **剔除两条**：官方的「柠檬茶」标着 412 kcal、碳水 97.5g/100g（看着像速溶粉而不是泡好的茶），
  「煎饼」碳水 83.8g（像山东干煎饼，不是天津煎饼果子）—— **拿不准就不加**。
- 顺手修了一个口语缺口：「昨晚」「今早」「明早」不在时间词表里，
  「昨晚吃了一份年糕」会**丢掉份量**（配到了年糕，却走分类兜底 200g 并标成估算）。
- 两个测试样本从「香辣鸡腿堡」换成「本店秘制小食」：前者现在真的算得出来了。

单测 161 → 163 全绿。

---

## [0.6.0] - 2026-09-18

**常见食材认得出来了，每条新数值都能追到出处。**

「一份包菜」记不上，根因不在解析、在**食物库**：库里 18 条素菜**全是成品菜**
（清炒时蔬、蒜蓉西兰花…），一条生的基础蔬菜都没有。这次补上。

- **新增 26 条基础食材**：卷心菜（别名 包菜 / 圆白菜 / 甘蓝）、生菜、油麦菜、大白菜、小白菜、
  番茄、黄瓜、胡萝卜、白萝卜、冬瓜、苦瓜、丝瓜、南瓜、西葫芦、青椒、洋葱、韭菜、西兰花、
  莲藕、山药、金针菇、鲜香菇、平菇、杏鲍菇、猪里脊、鸡胸肉。
- **数值来源可逐条复核**：取自**中国疾控中心营养与健康所**《中国食物成分表》查询平台，
  每条 `source` 里带网页编号；`data/foodSources.json` 是引用台账。该站声明数据仅限非商业使用，
  本项目是个人非商业项目；台账**只记引用标识、不复制表格数值**，要核对就按编号回页面查。
- **官方没检测的项目就留空**：这 26 条全部没有膳食纤维数据，一律保持"未知"而不是补 0 ——
  界面本来就单独报覆盖率，编一个 0 会让"今天纤维够了吗"变成假结论。
- **份量表补缺**：原来有 **19 条食物没有任何份量规则**（写「一份草莓」「一个土豆」会静默走
  分类兜底的估算值），现在补齐；「15只饺子」曾经算成 15 份 3000g（份量表里没有 `只[饺子]`），
  现在是 300g。顺带修了一个规则顺序错误：「一个蛋糕」以前会命中「鸡蛋」那条拿 55g。
- **两道新闸门**：`npm run check:reference` 校验「写了外部来源的条目必须有台账」；
  `check:nutrition` 新增**份量表联动校验**（每条食物至少要有一条份量规则命中它）。
  两道新检查都**逐条自证过会失败**。
- **泛称走单独一条路**：「一份肉」「一份蔬菜」不再假装"库里没有"，而是明说**太笼统**，
  并按荤菜 / 素菜给候选 —— **绝不猜**成猪肉或牛肉。猜错的后果是账本上多一条数值正常、
  但根本不是他吃的东西的记录。

单测 159 → 161 全绿。食物库 184 → **210** 条，份量规则 82 → **103** 条。

---

## [0.5.3] - 2026-09-18

**修：「一份饭两份肉一份包菜」这种连写认得了。**

中文不习惯给每样东西都打标点，一句话里连着说三样是最常见的形态之一。
以前整段进解析，食物名就成了「饭两份肉一份包菜」—— 查库必然失败，
界面上只说一句「库里没有」，而用户明明把三样都说清楚了。

同一类里还有个更隐蔽的：「一碗米饭一个鸡蛋」**只记下米饭，鸡蛋那份直接消失**。

- `splitFragments` 多了一步：按标点/空白/连接词切完之后，每一段里**再按「数量 + 量词」切**。
- ⚠️ **至少要出现两个「数量+量词」才切**：「三杯鸡」「一份包菜」都只有一个，
  切了反而把食物名切坏。
- 第一个数量词之前的内容（「我吃了」）会单独留一段，它剥掉噪音后是空的，
  于是在上层被丢掉，不会变成一条垃圾记录。

单测 157 → 159 全绿。纯解析改动，单测就够。

> ⚠️ 这一步**不解决**"库里没有这个食物"：`一份肉`、`一份包菜` 现在会各占一行并明说
> 「库里没有」，同时给出相近项让你挑 —— 这是对的，硬配一条数值不对的反而更糟。
> 「包菜」这类常见菜要靠食物库扩容补进来（路线图 P6.1）。

---

## [0.5.2] - 2026-09-18

**「记一笔」又能多听懂四种说法。**

先说共同点：这四种都是用户明明说清楚了，账本却**静默算错**、或者干脆丢掉一份 ——
而界面上看起来一切正常。这是这个模块最不能犯的错，所以每一处都补了一条
"不该切 / 不该粘"的反例测试钉住。

- **「米饭和红烧肉」真的记成两条了。** 连接词「和 / 跟 / 加 / 以及 / 还有」与顿号是同一件事，
  现在一起切。以前整段进解析，只认出「米饭」—— 红烧肉那一份热量凭空消失。
  「一包薯片 一杯奶茶」照旧切两段。
- **「两三个鸡蛋」按保守值记成 2，并标成「估算」。** 含糊相加（→5）会**系统性高估**，
  而用户说的是"大概"；屏幕上也不该出现一个看起来精确的 2。
- **「一打鸡蛋」= 12 个。**「打」是固定的计数单位（12），不是跟食物相关的份量，
  所以在解析出口就展开成「个」，走份量表里现成的 `个[鸡蛋]` —— 数字来路没变。
  以前整段认不出来（「打」既不在量词表里、也不是数字），只能兜底成"一份鸡蛋"。
- **「饺子15个」认得了。** 中文里份量写在后面一样常见，原来只认前置，
  后置的退化成"没写份量"→ 分类兜底 200g（真实 300g），界面上看不出任何异常。

单测 149 → 157 全绿。仍然是**纯解析改动，单测就够**（不经过界面，冒烟抓不到）。

---

## [0.5.1] - 2026-09-18

**修：「15 个饺子」被记成一条叫「5」的假食物 + 1 个饺子。**

在数字和量词之间打个空格（「15 个饺子」）就会被切成两段：
前一段解析出一个叫「5」的东西（界面上显示「「5」· 库里没有」），后一段只剩「1 个饺子」。
用户明明说了 15 个，账本上记成 20g —— 差 15 倍，而那条凭空冒出来的假食物比真错误还显眼。

根因是切段前的空白粘合只认**度量单位**（克 / g / ml），不认**量词**（个 / 包 / 杯），
所以「15 个饺子」里的那个空格被当成了「两样东西」的分隔。

同一批还修掉一个更隐蔽的：中文数量词原来是个**单字字符类**，
匹配「十五」时只吃下「十」、剩下的「五」进了食物名 ——
**15 个饺子被算成 10 份（2000g）**，比真实值大六倍多，而且一声不吭。
「二十」「二十五」「三十」同理。

- 「数字 + 量词」之间的空白现在会先粘掉再切段；「一包薯片 一杯奶茶」这种
  **两样东西**之间的空白仍然照切（回归测试盯着这条）。
- 中文数量词认到两位数（十 / 十五 / 二十 / 二十五 / 三十）。
- 「两三」这类**约数连写仍然不认**：含糊相加（→5）比不认更糟，取中值还是保守值是另一个决定。
- 数量词认不出来时退回「整串当食物名」，绝不让 `amount` 变成 `NaN` ——
  那会一路写进记录，变成账本里谁也解释不了的数。

新增 5 条单测（144 → 149 全绿），单测就够：这是纯解析，不经过界面。

---

## [0.5.0] - 2026-09-18

**「帮我挑」：填了 DeepSeek Key 之后，首页那张推荐卡可以从你自己的菜单里挑几道。**

以前「今天还该吃点啥」只推本地算出来的东西（食物库 + 你的菜单库一起打分）。
现在多了一条路：点一下「✨ 帮我挑」，模型从**你的菜单库**里挑 2~3 道，各配一句理由 ——
它比本地那套多知道的是"你今天偏了什么"该怎么用一句人话说出来，
以及在你几十道菜里挑哪几道更合适。

**它一个营养数字都不产生。** 屏幕上的热量仍然是本地按食物库估的区间，
模型的措辞里出现的数字会被直接摘掉。这是刻意的：模型一旦吐数字，
就等于凭空多了一个没人审计过的第二数字来源，而用户会拿它当账算。

- 新增 `lib/ai/`：`deepseek.ts`（浏览器直连的调用封装，Key 由调用方传入，
  这一层不碰 localStorage，所以能在 Node 里被单测）+ `recommend.ts`（prompt 组装、白名单过滤、措辞去数字）。
- **没填 Key 就不显示这个按钮**，也不报错；调用失败或挑不出来，自动退回原来的本地推荐。
- 模型只能回答"菜单里的第几道"（序号必须落在候选范围内），名字对不上的一律丢弃 ——
  它编不出菜单里没有的菜。
- 「饮食建议」的每条规则多了一句**纯定性**的描述（`hint`），喂给模型的是它而不是带数字的 fact；
  两处共用同一套阈值，不会出现"建议卡说钠偏高、prompt 说钠正常"。

---

## [0.4.0] - 2026-09-18

**记一笔多了「一顿饭」：点一下就落下整整一餐。**

以前「吃了顿饭」这句话只能被拦下来问「具体吃了什么」，或者靠用户自己一条一条记。
现在点「一顿饭」，选一个常见搭配（盖饭 / 一菜一汤 / 两菜一汤 / 小火锅 / 丰盛），
自动展开成「米饭 + 荤菜 + 素菜 + 汤」等多条，可以逐条删掉不吃的、改克数，
选好餐次后一次记下。

- 新增「一顿饭」入口与 5 个预设搭配；展开后复用既有的预览 / 删改 / 餐次选择，零新增概念。
- 数据层新增 `recordDietEntries`（批量写）：以前记 N 条要读写 N 次本地存储，现在一次搞定。
- 预设里只有「食物 + 克数」，营养值仍由 `nutritionOf` 现算 —— 数字来路没变。

---

## [0.3.0] - 2026-09-18

**饮食页改成以「早中晚餐」为主，并把「为什么没记上」说清楚。**

这三件事都是上手第一天就会撞到的：记的时候不知道记进了哪一餐、记不上的东西只回一句「未匹配」、
以及一个「克数碰巧对、单位是错的」的隐蔽 bug。

### 新增

- **记一笔先选餐次。** 以前餐次是按当前时间推的 —— 半夜补记中午那顿会被算成「加餐」，
  而当天的三餐结构恰恰是这个页面要说的事。现在四餐都能选，默认按时间猜、随时可改。
- **饮食页改成以三餐为主。** 每餐给出本餐热量与占今日目标的比例，
  三餐参考分配（早 25% / 午 40% / 晚 35%）**明确标注是「参考」**——
  真实作息里有人不吃早饭、有人晚饭才是主餐，拿一个比例去判定对错是没有根据的指责。
  当日总览（含钠覆盖度、参考值标注等）完整保留，只是退到三餐之后。

### 修复

- **「吃了个苹果」在界面上显示成「200 份」。** 根因是前缀噪音表里写了「吃了个」——
  剥噪音时把量词「个」**一起剥掉**了，于是解析成"没写份量"、按分类兜底 200g 并标成「份」。
  而份量表里明明有 `个[苹果] = 200g`。**数值恰好一样，所以只有单位和「估算」标签是错的**，
  这种「看起来对」的错最难发现。现在只剥动词（「吃了」）、量词留给解析用，
  并且输入框里的数字标成「克」、份量另起一段显示（`克 ≈ 1 个`）。
- **「一碗饭」「一碗面」记不上。** 库里没有「饭」「面」这两个别名（只有「白米饭」「蒸米饭」）。
  加了别名指向**既有的**米饭/面条条目 —— **没有新增任何数值**，只改了名字怎么找到这一条。
- **加了单字别名之后，「午饭吃了红烧肉」被记成了米饭。** 模糊检索最弱的一档是「被查询包含」，
  而同分时名字短的优先，于是单字别名会稳定地抢走更长、更具体的匹配。
  现在这一档不收单字名字；单字仍可走精确匹配（说「饭」就是米饭，说「醋」就是醋）。

### 其他

- **「没匹配上」不再是一句干巴巴的「未匹配」**，改成说清是哪一种，出路各不相同：
  不需要记（水、清茶 → 并指向首页的喝水打卡）、说得太笼统（「吃了顿饭」→ 要落到具体食物）、
  只有份量没说是吃什么、库里确实没有。
- 解析结果那一行不再是「原句 → 剥完的句子」这种机器视角，
  改成「剥掉了「晚上吃了」· 1 × 「一包」70g = 70g」—— 用户能顺着这句话核对。

### 闸门

- 冒烟新增端到端检查「记到某一餐就落在那一餐」。
  ⚠️ **它的第一版是假的**：写死「记到午餐」，而冒烟常在中午跑，
  `recordDietEntry` 按时间兜底恰好也得出「午餐」—— 把餐次传参打断之后它照样全绿。
  现在改成先读默认选中项（那就是按时间猜的结果）、再刻意选一个**别的**餐次，
  并把「故意打断」这一步做成自证：打断后必须红、退出码 1。

---

## [0.2.1] - 2026-09-18

**修掉三个「账本默默记错」的毛病。** 都不是新功能 —— 是已经在线上、用户一上手就撞到的问题。

### 修复

- **「一包 70g 的薯片」这类写法会算成完全不相干的东西。**
  用户既说了份量又说了克数时，旧解析只取走 `70g`、把「一包」留进食物名，
  查库失败后退化成按分类兜底的估算 —— 实测「一包 70g 的薯片」被记成 **「肉包 200g」**。
  现在两个信息都用上：`1 包 × 70g = 70g`，并在界面上写明这个算式。
  （`两包70g的薯片` → 140g，之前同样只算 70g。）
- **空白会把一句话切碎。** `薯片 70 克` 被切成 `["薯片","70","克"]` 三段，
  第一段没份量走兜底（50g）、后两段又凑不出食物名。
  现在切段之前先把「数量 / 克数 / 单位」之间的空白粘掉，`薯片 70 克` → 70g。
- **只有量词时会乱配食物。** 输入「一包」这种只有份量没有食物的片段，
  正则回溯会把「包」留作食物名，而模糊检索会把它配成**任意一个含该字的食物**
  （实测命中「肉包」）。现在量词一律归位成量词、食物名留空，
  并明确说「只有份量、没说是吃什么」；单字不再参与模糊检索。
- **进度环第一次点击没有动画。** 0% 时那根弧线原本**根本不渲染**，
  第一次点击时它是首次挂载，而 CSS transition 不会在挂载时播放 ——
  表现为「第一次点没反应，第二次之后才有」。改成始终挂载 + `strokeDashoffset` 过渡。

### 改动

- 菜单库里估不出成分的菜，**把已经认出来的食材做成按钮**（「就是『米线』」）。
  以前只给一句「关联一下」，用户得自己在搜索框里重打一遍菜名，绝大多数人到这一步就放弃了。
  认出来的这批**不参与任何数字**，只是线索。
- 单测从 98 条到 108 条，上面每一条修复都有一条对应的回归断言钉住。

---

## [0.2.0] - 2026-09-18

**能吃能算了。** 这一版把营养数据层（纯逻辑）和饮食界面一起放出来：
记一句「晚上一包薯片，一杯奶茶」就能落库，当天吃得怎么样、缺什么、下一口该吃点啥，
都在界面上说清楚。所有数字都只有一个来路 —— 查食物库再乘一次克数，模型不参与。

### 新增

- `lib/nutrition/` 营养层，**零 UI / 零 Next / 零 localStorage 依赖**：
  只接受入参、只返回结果（id 与时间戳由调用方传进来），
  因此能在 Node 里被直接调用、被单测、被闸门检查
  - `core.ts`：折算、求和、结构分析、与目标比对
  - `library.ts`：食物库索引与检索（4 级打分排序）
  - `parse.ts`：口语份量解析（剥时间词与动词、中文数字、量词对档位）
  - `targets.ts`：目标推导（**复用 `lib/health.ts` 的热量目标**，没有另起一套公式）
- `data/foods.zh.json`：**自建 184 条中式食物库**，按每 100g（饮品每 100ml）给出
  热量 / 蛋白 / 脂肪 / 碳水 / 钠 / 纤维，每条都标注数据来源
- `data/foodPortions.json`：82 条份量规则（88 个档位）——
  「一包薯片」70g、「一杯奶茶」中杯 500ml、「一碗米饭」180g 熟重
- **饮食日记页**（`/diet/`）：导航新增第 5 个入口
  - **一句话记**：说一句「晚上吃了一包薯片，一杯奶茶」，逐段解析成待确认的条目。
    每一条都把**折算依据**摆在屏幕上（`1 × 「一包」70g = 70g`），
    没写份量的标「估算」，库里没匹配到的给几个相近的让用户改选 ——
    只显示一个数字，用户没得判断
  - **食物搜索**＋**手动选份量**：先定食物再定份量，两步不合并 ——
    合并的话点错一个食物就直接落库了，而"点错一个"会让当天的数字全歪
  - **当日概览**：热量环 + 三大营养素供能比 + 逐项对照目标
  - **质量分**（`score.ts`）与**建议卡**（`advice.ts`）
- `lib/storage/diet.ts`：饮食日记数据层（键 `recipe.dietLog.v1`）
- **菜单库挂上营养估算**（`menu.ts`）：菜单里的菜按菜名拆出食材再求和，
  给的是**区间**（`224~464 kcal（±35%）`）而不是一个确定数字 ——
  同名菜各家做法能差一倍，写死一个数是假精确
- **首页「今天还该吃点啥」**（`recommend.ts`）：把今日缺口与用户自己的菜单库
  一起作为输入，推的东西必须真的对得上缺口，理由里带上"补多少"的数字
- **周报加上一周饮食质量**：逐日分数 + 与上周比较 + 最好/最差那天
- **两道新闸门**：
  - `npm run check:nutrition`：食物库质检。核心是**闭合校验**
    （`蛋白×4 + 脂肪×9 + 碳水×4` 必须约等于标称热量），
    能抓住"数字看着像那么回事、比例却自相矛盾"的错误
    （黄油曾写成 100g 里含 98g 脂肪 + 0.9g 碳水、热量还 888）；支持 `YQ_SELFTEST=1` 自证会失败
  - `npm test`：营养层与数据层的单测，覆盖 **「没有数据」≠「0」**、
    **记录里的营养值是快照**、**数字只能来自一次乘法** 这类"算错了也不报错、
    但结果就是错的"规则
- `npm run probe -- --text "..."`：把一条口语输入逐项打出来（食物 id、折算依据、数据出处）
- `scripts/run-ts.mjs`：编译 TS 到 `.tmp-check/` 后交给纯 Node 跑，
  合并了原先重复的两个运行器（`run-continuity.mjs` 已删除）

### 修复

- `scripts/deploy.mjs` 的 `DEPLOY_REMOTE` 默认值由 `mine` 改为 `origin`：
  仓库里只有 `origin`，旧默认值会让发布在推送那一步直接失败
- 口语解析曾把「晚上吃了一包薯片」的「一包」认漏，悄悄退化成按分类兜底的估算值。
  根因是剥前缀噪音时**短词排在长词前面**，「吃了」被剥成「了」——
  长词必须排在短词前。现在探针能一眼看出：正确时是「折算：1 ×『一包』70g」，
  退化时是「按分类兜底 50g（估算）」
- **部分覆盖的汇总数字以往像"确定值"**：库里 184 条食物都有钠，但有 110 条没有纤维数据。
  一条只有钠没有纤维的记录汇总出来的"膳食纤维 3g"，读起来像真的，其实是残缺的。
  现在覆盖率不足 90% 时，那一行会直接写明「只基于 X% 的记录」

### 明确不做的事

- 不让模型输出任何营养数字。数字只有一条来路：`nutritionOf(食物, 克数)`。
  模型只负责「听懂」与「措辞」，且食物必须落在候选集内、克数必须在合理区间。
- 不把份量表复制进 `FoodItem`。唯一来源是 `data/foodPortions.json`，两份必然漂移。
- **估不出来就不给数字。** 菜单里「黄焖鸡米饭」只能命中「米饭」，算出来两百出头，
  而真实的是六百多 —— 连区间上沿都够不着。这种情况现在**拒绝估算**，
  让用户手动关联一次，而不是给个看起来精确的错数。
- **今天还没吃完时不说"营养不够"。** 只记了 400kcal 的一天，纤维必然"不足"、
  蛋白必然"不够"，但用户还没吃晚饭。这类"不足型"问题一律要求当天热量已到目标 60% 才报。

### 已知限制

- **食物库只能被闸门证明「算术自洽」，不能被证明「数值全对」。**
  数值错但比例自洽的情况（例如某种菜的钠记成 1/10）需要另一套参照集人工校核。
- 184 条覆盖常见中式家常与外卖；冷门食材会落到分类兜底值，界面上标出「估算」。
- **菜单库的菜名能自动估出成分的只是一部分**（随包的 12 道示例里能估出 4 道）。
  剩下的不是算错，是**不估** —— 用户关联一次食物即可，之后这道菜就走查表口径。
  食物库目前偏"食材"（有面条、卤牛肉），缺"菜名级"的词（拉面、麻辣烫、汉堡），
  这是覆盖率的主要瓶颈。
- **建议与推荐都还没接模型。** 现在的文案全部由规则引擎算出（事实与数字都在里面），
  P4 计划里"模型只做措辞"的那一层尚未接线。
- 质量分**没有「添加糖」与「超加工」两个维度**：食物库没有糖字段，
  硬凑一个出来正是这个项目最反对的假精确。「零食甜饮」是分类口径的代理指标，已在界面上写明。
- 步数只能手动录入；网页架构下微信运动 / HealthKit / Health Connect 都拿不到。

---

## [0.1.0] - 2026-09-17

**元气账本首个版本。**

`recipe.*` 这套 localStorage 键名与 JSON 结构**逐字保留**，因此已经记录下来的
全部数据可以直接延续 —— 不需要迁移，是接着用。

### 新增

- 项目骨架：Next.js 16 App Router + TypeScript 5 + Tailwind 4，静态导出，PWA
- **全新视觉系统**：宣纸白底 + 竹青主色 + 暖橙点缀（`--yq-*` 令牌，深浅两套对称）
- **应用图标与 iOS 启动图**：由 `scripts/make-icons.py` 用标准库生成，无第三方素材
- 数据层 `lib/storage/`：18 个键名契约、读写原语、各领域 CRUD
- 备份 `lib/storage/backup.ts`：导出（可选含 AI Key）、导入（**合并 / 覆盖**两种模式）、
  导入前内容预览、清空
- 健康小屋：`HealthProfileCard`（档案 → 目标）、`CheckinCard`（喝水/步数/睡眠/心情）、
  `WeightCard` + `WeightChart`、`ExerciseCard`、`BadgeWall`
- 菜单库：搜索、按商家分组、加菜/改菜/删菜、改商家名（自动去重合并）、单槽撤销
- 周报：打卡达标率、7 天柱状图、运动统计、体重变化
- 数据总览：一眼看出既有数据有没有缺
- `scripts/verify-subpath.py`：子路径自检（静态分析 + 可选 HTTP 探测）；
  漏了 `BASE_PATH` 构建时会直接说明根因，不再刷屏报误报
- `scripts/deploy.mjs`：版本三处校验 → 构建 → 注入 SW 版本号 → 推 gh-pages → 打 tag
- **两道数据闸门**（详见 README）：
  - `npm run check:data`：把既有结构的数据喂进数据层，验证键名、字段名、嵌套结构都读得出来。
    跑的是 `lib/` 里的真实读写函数（不是复制一份逻辑），并支持 `YQ_SELFTEST=1` 自证会失败
  - `npm run smoke`：用本机 Edge 的调试协议真实打开构建产物、写入数据、
    读回屏幕上的文字与输入框并截图。零额外依赖，不引入 Playwright
  - 两者共用同一份样本 `scripts/fixtures/legacy-v1.json`，不会各写一套而漂移

### 修复

- `ExerciseCard` 写入 / 删除运动记录后没有广播数据变更，同一页的其它卡片会停在旧数据
- `SettingsDialog` 改杯子容量、合并导入备份后同样没有广播
- 设置页有一处文案把 markdown 粗体语法写进了 JSX，界面上会原样显示星号
- `lib/storage/takeout.ts` 的种子数据导入由 `@/` 别名改为相对路径：
  `lib/` 是不依赖打包器的领域层，改成相对路径才能被纯 Node 脚本直接跑

### 结构调整

- **页面只做编排，不放业务 state。** 状态一旦全堆在页面里，一个文件能长到七八百行、
  十几个 `useState`，改一处要通读全场；现在每张卡自管状态、通过回调通知父级。
- 视觉自成一套：宣纸白 + 竹青 + 暖橙，深浅两套对称，刻意避开常见的
  「暖橙大面积 + 浅蓝辅助」配色。
- 示例菜单库为自撰内容；提示词与文档随代码一起维护。

### 明确不做的事

- 不做「库为空就重新灌示例数据」—— 那会让用户删光后一刷新示例又回来。
  恢复示例库只能走显式按钮。
- 不让模型直接吐热量数字。数值必须查表、过校验（这条在饮食模块落地时执行）。

### 已知限制

- 饮食日记与营养分析尚未实现（下一步）
- 步数只能手动录入；网页架构下微信运动 / HealthKit / Health Connect 都拿不到
- iOS Safari 不支持 `BarcodeDetector`，扫条码要等原生容器或手动输入
